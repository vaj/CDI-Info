End_to_End_Management_of_a_CXL_enabled_Datacenter_Memory_Fabric

Good morning. This is Breiner from H Platform. We have been building the memory appliance for like three years. So finally it's going to production in one of the CSP last month.
So later I will explain uh what's the uh challenge that we face. But before that I will give you an introduction of the uh the product. It will be short, but uh just that you see what has been deployed in uh real CSP environments. So there's uh four uh compute nodes.
Going through the CDAP cables, of course, running the CSO protocol, going to the memory appliance. Then in that memory appliance, there's a lot of CSO memory modules that can be a dynamic assigned to the compute node. In in the real uh case, what they do is that
They use all shared memory. So it means that all the four servers see exactly the same memory address of a CXO memory. So it means that the first CXO memory modules is exactly the same as memory, I mean in the logical memory address in the server size.
exactly the same for the server one, server two, server three and server four. Then it then it would be much easier for the uh user to use it because otherwise it will get confused uh which which section is uh which sections. So they use exactly the same for server have uh uh local DDR, for example, like five hundred twelve.
DDR in each servers. And then they have 12 CSO memory modules from 1 to 12. It's by sequentials to be assigned to the four different servers. So physical-wise, the address is the same. Logical-wise, the address is the same. So they use the shared kind.
Of our architecture to use this solution. So then come to the implementation challenge. Before talking about that, because you know that every CPU, because it's shared memory, so from our point of view, that you want to see it.
In the logical memory mapping to the physical memory. But in Intel and AMD, they have a different way to treat it a physical memory address mapping. For example, Intel reserved two gig in the the very first memory address. So it's starting from that
thirty thirty gigs uh is a the real memory address. But the AM D doing the different way, AM D reserve like twelve gig uh close to the uh one tera uh space. It will create some of the challenging because if you assign a memory if you assign it's actually close than the one tera.
A single memory module will be cut into two pieces. But from our point of view, we know it, but customers sometimes will get confused. Why is that? So that is a little bit challenging for the memory address. Logical memory address mapping to the face.
And of course, all the BIOS settings you need to implementation-wise for the Intel and the AMD have different BIOS implementations. You've got to have different settings to enable the CXO's capabilities. Then, the second thing is that all the servers, when they design,
The generic servers. They don't expect that you will put a retirement card and expand the car to another chassis. And that is running as the PCI physical layer. So then we'll create a lot of problems that say, hey, if you put that
Retirement, then the boot up schedules, the low firmware timing will create some of the challenges. So, in generic servers, it will be a little bit challenging that you put the retirement everywhere. Then, in different slides, different behavior because they give.
The 12 volt power is different. And the timing sequence for the retirements to load the firmware and to start training the servers is a slightly different in different server, even in a single server. It's different a lot. It's a it's also different. So then I mentioned that you have lots of
Signal qualities, auto-negotiation kind of issues. So that is also a kind of a challenge that we have in the customer side. And then because in the past, all the memory controllers are located in the CPU, right? So CPU.
take care of everything. But now you put this the the memory controllers in the CX or memory modules in another chassis. So everything happens, it will report back to the CPU because otherwise it will get confused of what's going on. For example, if the the link is failed,
It will be translated to the CPU. Then CPU knows, oh, there's some event, say, hey, the link is failed. Or if there's some poison data, he needs to know that data is poisoned. And it should be reported by the memory controller and all the way go through the switch and go to the CPU. So that probably
Is we work with different memory controllers, vendors to integrate that tools to inject errors, make sure everything's will be 100% correct so that CPU understands what's going on. But you know that different memory controllers will different.
Implementation way and there are different behavior. So we need to do some kind of tweets to make sure everything works fine. And because of the implementation of the CXO switch, they do not
They use a memory mapping, so they do not decode the DVSec IDA. But if in the CXO2, you need to do that in order to enable the CSO capabilities. So we got to blocklist the CSO driver in order to avoid that kind of issues.
And of course, in the OS levels, we also are using 6.2 or later to make it work. Because different kernels have different sets of memory drivers. Before 6.2, that is kind of a POC version. But after that, currently we use 6.11 as our memory.
Main OS to distribute the to deploy the CXO solutions. Okay. So because CXO, I talk so many challenges, right? Because CXO is a new technology. It's involved in the memory controllers.
Involved in the OS, CPU platform, retimers, OS, and of course, above the applications. Everything is being involved. So you got to make sure how you want to deploy it in the market. There is a way to do it because in our
Deployment they use in memory database, a very specific and confirmed, very friendly usage case. So they can use all the memory as a shared memory for the memory database. So the customer needs to handle like OS kind of modification and application.
Wise kind of software cache coherence kind of things. And then it choose a specific CPU platform and choose a specific server. And of course, then work with the retimers to make sure all the connections, everything is 100% correct. So that is the way that they do it.
So we have successfully helped the customers to go productions. And in in in this production, I think it's there's a two things that's also very important because customer has a determination to get every uh vendors to work together like uh memory controllers.
Like retirements, CPU vendors, and of course S3 platforms to provide a software. So everybody will sit down to just to make sure it works. So this is the first scene. The second thing that the customer needs to, I should say, use some work.
Around and understand it won't be sent as a spec. You just some kind of walk around and uh to make sure that it could be work in their in their environment. W with this kind of conditions, I think it's CXO can work very well in the customer side. And they will they will they will go productions, uh they already go productions and I think it's uh
In the future, they are deploying more and more in memory database. That's my talk. Thank you very much.
Good morning, everyone. Thank you for joining this session. Today, my talk is how about this memory disaggregation technology?
Specifically, one based on genuine CXL AG switch. My name is Andre, principal engineer in the memory architecture group at Samsung. So, why is the software defined memory?
is so important. As you know that the well resource underutilization is the persistent and costly issue. This is especially for memory. Listen to analysis of CAST AI report that
on average, only 23% of the cluster's memory is actually being used. The root cause of the low utilization is difficult in accurately uh predicting actual memory usage, which forces us to overprovision.
to avoid the dreaded out of memory. As you know, this is precisely why the composable memory system is required to recover all stranded memory. Here is the
This is the memory pooling reference architecture we developed with HSVP as Samsung Memory Research Center. We call this CMS or compatible memory system. To enhance memory utilization, we have two different references.
Architecture. The diagram on the left shows CXL memory pool built using RDMA. That's what we introduced last year. This year, we introduced the genuine CXL memory pooling reference architecture.
Built with CXL switch. Uh compared to the LDMA-based approach, the CXL-based solution used load-store semantic access, which allowed the host to directly access the memory. This is what the delivery does in significant
peak and performance benefit. We conducted an evaluation of the performance in memory database application in each this environment based on the YCSV benchmark.
Experimental setup utilized 20% host local memory and 8% remote memory, Oxfess PR, either RDMA or CXL switch. When the host local memory became insufficient during the application,
Execution, the relatively less used code data residing memory was migrate or swap out the remote memory. The results are impressive. In case of the memcache D, the
you know memory pool environment leveraging CXL switch the throughput increased by the 4.6 time compared to the NPN band and up to six point two time compared to rocket bur version two. Tail latency also
So dramatic improvement, uh reducing by eighty two percent compared to InfiniBand and eighty seven compared to Rocky Virgin True. We obser uh observed that uh exact the same trend in Redis in memory T V application also.
Uh to make the best use of the DCL switch-based memory pooling hardware, we implemented the memory fabric orchestration requirement defined by OCP, which is uh referred to as the DCMFM.
And also this implementation is what we call the Samsung DC MFM software. Samsung software DCMFM software is the REST pool API based memory pooling management plane. Uh it discovers host, switch and the CMM device visualizing
The topology and exposes API for dynamic application and the deal location. Similarly, we integrate the CXL memory resource with Kubernetes. As you know, the Kubernetes is the major problem.
Container orchestrator in data center. Despite its widespread adoption, each internal for native resources are based on the hardware era that's over 10 years old. This means that Kubernetes lacks readiness for
CXL-based memory disaggregation. Let's look at two key issues, the left-side diagram. First, in place dynamic node design issue. Kubernetes lacks the capability to detect and manage memory disagreements.
that are usually edited to worker node, like CXL attached memory and shown in figure. Furthermore, the since this CXL memory is often represented as the CPList NUMA node, it breaks Kubernetes assumption that memory
should be co-located with CPU. This limitation is prevent efficient scheduling and uh utilization of the dynamically added memory without restarting a critical component of relying on external workground.
Uh second is the tiered memory support for special applications. Kubernetes doesn't provide the native way for application to express preference or requirement for a specific memory type. For instance, uh choosing DDL5 didn't for
Hard data and the CXL data for cold data. As a result, the application has no guaranteed way to influence memory or location deci decision based on the performance of a latent requirement. Uh the key feature of our DCS
Software is that it enables dynamic memory capacity on Kubernetes worker node, overcoming the two issues I just mentioned. In our target Kubernetes use case, when a container part is scheduled and the worker node is running low on memory, DCM is a very good thing.
Later, the when the pod is terminated, the memory is returned to the pool. This way the memory resources are used efficiently on the amount needed and at the right
Place and time. Uh let me explain explain in more detail. The manager is the memory orchestrator that manages the entire memory pool shared through the switch. The CXL agent is abstraction related to the memory.
That handle the vendor-specific switch fabric management interface. According to the Kubernetes structure, we have a two component: agent and monitor. The agent on the work node is based for the container lifecycle event, such as the pod request.
Memory. The monitor resides on uh master node, which is gathering point for part execution completion event. And it uses those events to release the memory pulled back to the pool. Okay now.
So let me show you demo live. Uh illustrate exactly how this CMF works. This scenario is show uh paths that are scheduled on the server with and without the CMS hardware and software stack.
First uh let's look at. Without the CX and the other one, the other one is a very
Only the five part parties can be scheduled after that OEM error occurred. And no additional parties can be deployed. In contrast, the center of the r uh right part of the video, you can
That uh with this MFM, we can schedule the three additional parts beyond the the initial vibe. Sorry.
Yeah. This is possible because the DCMFM dynamically allocates memory from the CXN memory pool to the worker node.
In addition, DCMM can provide the memory allocation polish options for each part, allowing the system to select either local DRAM or CXL memory. Uh after most case, the you can see that the
uh memory allocated from the selecting the local DRAM or alternatively the CXM memory. Yeah. Additionally we collaborate with the HH3P to implement the World Port virtual switching
Technology. Unlike the previous demo in this switch configuration, only the upstream port is a separate worker node. All the subsequent memory binding is performed dynamically. You will see that five parties are scheduled.
On worker node, and each part requests 64 gigabytes of memory. With each added numbered part is scheduled, 128 gigabytes CMMD bind operation is performed. When an even numbered part is available, the number of the number
Is scheduled, it receives its memory allocation from the already bounded CMD. Okay, let's uh let's see.
This enabled the Linux Dynamics Copest feature and allowed the
binding and unbinding of the logical device, ensuring in efficient memory resource management, including hub plug and removal within the C Excel switch.
So far, I have introduced Samsung Research on Memory Pooling Solution. Next, our partner, Leded, will be give a presentation on the CXL Memory Pool Management Solution software, which is based on Samsung CXL Memory Pool Reference Architecture. Thank you.
Hi everyone. I'm Tan Lee from Red Hat. Here I may briefly introduce the CXL memory pool enabler developed through
Sorry. They developed through the Samsung Electronics and Red Hat's Sixth Memory Ecosystem collaboration. This enabler communicates with the CXL Fabric Switch through APIs, and each host runs a SixL.
Agent demon that detects memory usage and triggers real-time expansion when its local capacity runs out. In our demo, virtual machines seamlessly extend memory beyond DRAM limits by pulling from the remote sexual memory pool. And when
When stop-up conditions occur, cold pages are demoted to faster Sixcell memory instead of disk to improving latency and system stability. This SixL memory enabler is evolving into a next-generation host memory management tool.
Bridging the local and remote CXL resources in one unified platform. Thank you.
Good morning, everyone. My name is Sumit Puri. I'm from Liquid, and today we're going to talk about end-to-end solutions built around CXL. And when we think about end-to-end, first we'll talk about the various building blocks that are available as we build these large
Scaling and pooling solutions around CXL memory. There's obviously the expansion chassis when we're pooling and sharing. This is the thing that the CXL memory devices go into. There's the adapter card that connects to the host. There's optical cables that allow you to connect everything together. There's switching that allows you to build.
With even larger topologies. There's a wide variety of memory modules that we can actually deploy inside the solution. And then, as was mentioned earlier, there's this concept of the fabric manager that allows you to orchestrate and move the memory around. Very simple topologies, we don't do this alone. This is an ecosystem opportunity for all of us. It requires
Many building blocks to deliver the entire pooling and sharing solution. In addition to the expansion chassis, the fabric manager, we actually have the memory modules. We have a variety of vendors that build devices that can go into these solutions. And then we also have the server vendors. As Brian mentioned earlier in our call, we can't do this without modifications to the servers that attach to.
To this, BIOS modifications, clocking modifications, modifications so that they can recognize things like 100 terabytes of memory attached to it. And so small topologies in our universe look like a handful of servers connected to one of these expansion pools. And then we can build larger topologies where we have many servers, dozens of servers.
Connected to many of our resource expansion chassis, so we can build very large architectures around this pooling and sharing concept of CXL memory. But regardless what the architecture looks like, there's a fundamental reason why somebody takes this journey of CXL pooling and sharing. And I'll show you a couple of examples.
Talking quickly because we only have about four minutes to get through these slides, so forgive me, but one of these vectors must be important in your environment in order to take this journey. You have to have a performance reason, a cost reason, or this concept of agility and flexibility has to be important in your environment. We'll walk through a couple of each.
And performance is one of the easier ones to understand. The server itself is only limited by a certain amount of memory that you can actually place inside the server. If we can increase the amount of footprint that is available to that server, there are many applications in the world where, if you give it more memory, it will just run faster, large swaths of them.
And so, one benefit is to be able to expand the memory capability of the server and make the application go faster. The other vector of performance that a lot of people don't think about is bandwidth expansion. One really good use case for CXL is expanding the amount of memory bandwidth that you have in and out of the CPU. Each one of those adapter cards that we have
We showed earlier, that's a by 16 Gen5 connection. We can have two, four, potentially eight of those connections inside of a server, drastically increasing the memory bandwidth in and out of the machine. And so, one way to think about CXL is not only capacity expansion, but memory bandwidth expansion.
Is important, right? It's very difficult for our enterprise customers to understand how much memory does my application actually need. We talked about this concept of a fabric manager and the ability to dynamically change the memory footprint. Today, the model is over-provisioned. I don't know what I'm going to need over time, so I just give it more. It's a very expensive way to approach the problem.
We don't do that with storage. We don't give every storage server a petabyte of storage. That wouldn't make sense. So, what if we could properly provision memory the way that we need it? The next one we're thinking about is virtualization. We think CXL has a really important role inside of VMware environments and virtualized environments.
Accounts are exploding. Our friends at Intel and AMD are now building CPUs that have gone from dozens of cores to hundreds of cores. Now there could be five, six, hundred cores inside of a server, but you only have 16 DIMS still. You're still limited on the amount of memory that you could attach to that machine. What if we could?
Could increase the amount of memory, unleash more VMs out of a single server without paying for more software? What if we could optimize not only the hardware but the software piece of it? Agility is important. The world is on the advent of optical interconnects everywhere. We are now at a point where we could have a centralized memory.
Rack, 100 terabytes inside of the row where we're serving servers to the left, servers to the right, with long reach optical interconnects. We think this is now upon us. And the question is: what are the workloads that are first going to benefit from this? We think one of the early workloads is going to be around database.
And one of the things around database is the way that it's done today is you build these large clustered environments in order to get the memory footprint that you need in your database world. What if we could collapse that? What if we could take an environment where it took six, eight, twelve servers to get the memory footprint that you needed before, and we consolidate that down to a single?
Server, buy less infrastructure, consume less power. Software is not free. What is the software implication of having to buy one software license versus having to buy a dozen software licenses? And performance. Today, everything in a clustered environment is a bunch of servers networked together.
There's a tremendous amount of performance loss every time we shard these databases and we spread it across multiple nodes, right? What if we could keep all that network traffic to zero and deploy the entire machine on a large, very big 100-terabyte system? Many times people talk about performance and label.
Latency. You know, what is the latency of disaggregated CXL versus onboard memory? And we think, yeah, there's a couple hundred additional nanoseconds when you disaggregate the memory into an external pool. Our first assertion is there's a large swath of applications that don't care. We don't care if you're 200 nanoseconds, 500 nanoseconds, we don't even care if you're a microsecond.
70 to 80 percent of the world's applications that as long as you're below a microsecond, it's not going to care. But a lot of times people forget that the next tier in the architecture after DRAM currently is NVMe storage. And NVMe is not 10 times slower than CXL, NVMe is a hundred times slower.
Than the next tier of memory. And so, the way that we think about CXL pools of large pools of memory is a very large bit bucket in a few hundred nanoseconds of latency, less than a microsecond, but it services a new tier inside the overall architecture. And we think that tier is going to be great for things like data.
In-memory database, graph analytics, where number one, we can put the entire database onto a single server, we can take that network latency to zero, we can move to an all-DRAM design as opposed to some type of slower media. And then this concept of multi-node memory sharing is also a
Upon us, where we'll have a talk later, where not only can you have a large pool of memory, but now we can have multiple servers at the same time sharing and accessing that memory pool. We think these are the layers that are going to get us not 10 times faster, but 100x performance improvement as we deploy CXL. Thank you.
