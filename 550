Large_CXL_Memory_Expansion_Systems_and_Workloads

Good afternoon, everyone. It's right after lunch, so thank you for staying awake. We'll go ahead and get started. My name is Sumit Puri. I'm from Liquid. I'm Ajezio Shi. I'm from Micron. Today we're going to talk about.
Memory pooling, sharing, and some of the workloads that we are implementing around that. CXL memory expansion is going to be the focus, and not only CXL memory expansion, but the pooling and sharing aspect of that. There's lots of slides in the world right now that talk about how.
Big the CXL market opportunity is going to be. This is not our data, this is from the Yole group. And they're predicting over the next couple of years this big growth in the CXL market opportunity. The question is: what is going to drive that CXL market opportunity? We see the CXL landscape in two forms.
One is what the industry is doing today, which is CXL memory expansion, where we take a CXL device, plug it into the server, and expand the memory footprint of that machine. What we are here to talk about is this concept of memory pooling and memory sharing, where we take many CXL devices, put it inside of a chassis, and then either put
Where we provision memory from that chassis to a server, or share, where we expose memory regions simultaneously to these servers and do a memory sharing architecture. We actually will do both, and Ajay will talk about some of the memory-sharing workloads that we're able to achieve. These are the building blocks required.
To go off and achieve memory pooling and sharing. You have an expansion chassis that holds the memory. You have the switch that allows you to build big architectures. You have the host adapter card that plugs inside the server. You have optical cables so we can go very long distances. And then you have orchestration software so you can assign memory to servers.
This is what simple memory sharing and pooling topologies look like, where we take an expansion chassis. Today, this can be up to 20 terabytes of memory. Soon, the capacities will go much higher. We can connect multiple servers over that, either through low-cost copper or optical interconnects for longer distances, and then implement.
A sharing algorithm. We can't do this alone. It requires our memory friends here to provide the CXL memory, and it requires the server vendors here also to deliver a complete solution. We can build very large architectures where we have dozens and dozens of servers connected to today hundreds of terabytes of memory, but we envision someday.
Petabyte racks of infrastructure, petabyte racks of memory connected throughout the data center over long-reach optical cables and allowing the ability to either build single servers with very large memory footprints or provision dynamically to improve things like DRAM utilization.
This is all working, it's working today, and it's important to understand where we think the memory latency will be able to be achieved on this kind of architecture. This is an example of us connecting 10 terabytes to a server over C Excel and achieving around 500 nanoseconds of latency with this kind of architecture. But if we look at the overall
Latency stack inside of the architecture. After we leave DRAM today, the next fastest thing that we have to go and speak with is actually NVMe storage. So when we look at some of these large database applications, once they're run out of DRAM to access for their environment, they have to make
To something that's not 10 times slower, that's actually 100 times slower. So, this is where we see this tier of memory applied: in that memory pooling and sharing area where very large pools of memory with a latency that's much faster than NVMe, but slightly slower than onboard DRAM.
Today, we're able to do simple demonstrations of this technology where we can have servers running and dynamically change the footprint of the memory connected to that server. But the more interesting thing that we're here to talk about today is complicated architectures that allow you to do things like memory sharing, where we can go off and have:
Have five terabytes or six terabytes or 20 terabytes or 100 terabytes of memory and then expose that memory to multiple servers at the same time. Server number one and server number two can access a very large memory pool and then do the simultaneous reading and writing of that.
To demonstrate that. Part of our ability to demonstrate that is in partnership with Micron, where today in the current CXL architecture, we don't have any hardware-level coherency. So, any coherency in this memory-sharing architecture has to be done at the software layer. And there's a lot of companies innovating in that space right now.
Jay will talk about what Micron is doing for FAMFS, but this is an example that we're showing where multiple servers can read and write to the same memory at the same time here. As we come to market, our vision is to bring this to market as an appliance where we can have many servers connected to many pools of memory and then share it.
Enable this shared architecture vision for not only performance, but also cost reduction. And this is my last slide here. And so, one of the things that we think is the first really interesting application for these large pools of memory is database. Things like graph database. And we see a
Multi-headed improvement on how we can improve not only performance, but cost related to these architectures. Number one is move from an NVMe-based solution to a pure DRAM-based solution and pick up multiple orders of latency improvement. Number two is get away from sharding architectures where you have to distribute the workload amongst multiple servers.
Build one large scaled-up server with a tremendous amount of memory and to avoid that network latency. And the benefit of all of this is we can do the multi-sharing. So once we've built that architecture, we can get the next level of improvement by enabling memory sharing across multiple servers.
That was fast. I'm sorry, I had a lot of slides. So appreciate it. Thanks, Sumit. As Sumit mentioned, one of the challenges is to how do you share this memory using a software-based interface where multiple servers can access this memory. There comes FAMFS, an open source scale-out memory file system from Micron.
So currently the superpower of memory is in uh low latency random access. So currently if you compare, uh there are only two tiers of memory, one is system RAM and the NVMe storage kind of a solutions. So the performance improvement when the when you add uh disaggregated memory in between there, then you can see it's generally a hundred x performance improvement.
Compared to the nearest tire, which would have taken is the storage. So that is where the biggest advantage of the disagreeable memory lies, because there is nothing existing right now. But this is one of the solutions which can be used to actually reduce the latency and workload which you want to do. So I'll skip this.
Large data sets generally don't just appear, they get wrangled. So you need to filter it out, you need to arrange it and organize structures and then place it out. Generally, the apps which do the wrangling for this use 2 to 5x of the memory capacity and avoid running out of the memory. So to do this, so they allocate a huge amount of memory compared to what you want.
But if you want to share it across, then if you can share it across and put the same data set in a shared memory, then that is where a lot of benefits can be achieved. Some of the things is like sharding can be avoided, you can avoid duplication of this data, and this file system interface is a quite natural interface for these kinds of applications. Now, but there is no memory-oriented file.
System is required, boom, there comes FAMFS. So, what FAMFS does is it aggregates disaggregated memory into files. So, you have a huge bunch of memory, so that can be organized as files, and you can use those files to read and write in the read and write to that. So, one of the things is with FAMFS,
Memory is accessible as files. So if you're doing a read and write of those files, it becomes a mem copy, or you can map that memory to your application and do load store instructions and do byte level or cache-lend accesses for that. So FAMFS files are memory and they're not storage. So, but whenever you want a faster access, faster
Analysis of data or sharing of this data, you can move this data from FS into in-memory indexing in in-memory accesses. And then after that, if you are, if you want to store this back into the persistent memory, you can move the data out from FAMFS to store persistently. So PASIX animations apply to FAMFS. So this is very, very with strict partnishing of data from separate files. So this is
One of the most important parts of FAMFS. So there is no change required in the architecture of apps to use this kind of interface. So orchestration layers such as PNFS can use FAMFS as a tier, so they can see a better memory performance and scale-out timing because of that. So, what do you do? How much, when do you decide that my data set needs to be disagreed?
Memory. So, one of the things is how much memory is too much. First, you figure out if all your data can fit into the system PRAM, fantastic. That's the way to go. But if you can't, can you do data reduction? So, if that is the case, then yes, the system memory is still an option for you. Can you shard this effectively so that you don't see any performance degradation? That's one of the ways to do it. The other way is to, if you have.
A sequential access. If you look at always the access is sequential, then you can use an RDMA DMA kind of an interface for doing sequential access. But none of this is possible. If your data is truly random accessible, then go ahead and put it in the disagreeable memory. It's generally, because of this disaggregated memory, it is a DRAM-based access. You will find a 100x performance improvement compared to the
Compared to the nearest tier, which is the NVMe storage. Again, the superpower of memory is in low-latency random access, so that is its superpower. With FAMFS, we can break the memory scaling barrier for graph analyte and rack pipelines or in-memory databases and indexes. So for rack pipelines, the transition is.
Happening from key value store to basically a graph database. So, a graph database is too much of sharing, sharding kind of a concept. So, this can directly be avoided if we're able to share the memory across the system with FAMFS. So, FAMFS use cases, wherever there's a data frame kind of a concept, then FAMFS can definitely be.
One of the tools there. So, wherever you see like pandas or RoxDB or any other application or Apache Arrow, all these apps mostly use data frames. And they already have an ecosystem where it already uses the shared data sets. Many of these use cases are read-only, and where you want the data.
Sharing across multiple clients. So that is where FAMFS is really useful for. Now, putting this into the shared memory, along with CXL as a CXL memory, enables you to run all your workloads and all the apps which do data wrangling or any other which uses data frames without any modifications.
So, what is the call for action? Attend the OCP Composable Memory System Workgroup on Fridays at 9 a.m. PST. Share your large memory workloads, challenges, what you face. Highlight your whatever you have, the success stories, and solutions, and requirements. Look at FAMFS. Please try to contribute to FAMFS. It's an open source.
And uh we would like to hear that what you have thoughts about it or usage about it. Follow up MFS on GitHub. And if you have any information or anything you you want us to uh specifically look at, we are ready to do that. Spread the word that disaggregated memory is not just a thing, it can be easily accessible as files. I think that's all I had. Thank you.
Questions? Any questions? Sumita I have the same question. Perhaps ask if you had a rack diagram there, right? And I wanted to just quickly check how this topology Yeah, no, absolutely.
Was this the one you're talking about? Or the actual rack rack? Wherever there is memory, yeah. So I see. So this is a memory rack being separate from the server rack? We do. So we have certain customers, especially in places like APJ, where rack power is extremely limited, right? We do this for GPUs.
Memory is the same exact concept. You can have your server fill, you can have your servers in one rack, and you can have an entire rack of memory in the other rack. One of the benefits is the shareability, the other thing is optimizing for the power footprint in some of these edge-type environments. But we have optical interconnects today, we can do 30 meters.
Per hop on optical disaggregation. If you visit the Acton booth, who is our manufacturing partner for a lot of this hardware, we are displaying multi-kilometer disaggregation over optics. And so by this time next year, we'll probably have a slide where we're showing multi-kilometer optical disaggregation, data center to data center.
Memory sharing and GPU sharing. So, in this topology, the CXL switch is on the right-hand side or left, or is it switch-to-switch in between? Yeah, so I'll give you a standard rack topology, which is here, right? And so, in this topology, what we have is we have five servers. We are not using a switch in the
This design because we are trying to maintain the lowest level of latency. If the reach is small, you actually don't need a switch. So, this is what we call a five by five, five servers connected to five of our expansion chassis. This gives you a hundred terabyte shared memory pool across five servers. If we need to build big
We add the CXL fabric switch to it, and then we can build larger topologies. This topology here, we're going to be in that somewhere in that 500 to 600 nanosecond range. When we add the switch, think another 200 to 300 nanoseconds of additional latency. Okay, thank you.
The switch, how can you have the multi-headed capability from, or are they no longer pooling, it's more expansion without the switch? So without the switch, so inside of each one of those expansion chassis, we have a switch. You have a switch. We have a switch. So there is a CCL switch. There is a CXL switch. When I say switch, I was referring to the top of rack.
Switch, where you have multi-level switching to build very large fabrics. Got it. Because there are some people doing, you know, external expansion without a switch, right? Which is also a valid solution, but it's not pooling or sharing. Whereas this is pooling and sharing, since you have a switch,
That's correct. Through via that switch is how we decide how much memory we are going to allocate to each server and it's also we enable the sharing part of it. I have a question about uh FEMFS. I think it looks like uh uh traditional RAM disk or RAM FS or
ZRAM, what is the difference between them and what is pro or cons? So uh FAMFS is basically uh segregated memory file system. So basically it doesn't it it is not like a RAM disk, it's a separate uh file system interface what is being created completely to allow you to do memory pooling and sharing kind of a concept.
So, that is FAMFS is what it does. So, it's not exactly as a RAM disk. But the advantage of having all this is you can have distributed load and you can have, like, for example, if you're running graph database applications and you're having the application, the database stored in the disagreement memory.
Then using the FAMFS interface, it'll just open that file and just do all your operations on that. How does this uh FAMFS access occur? Is it at the same storage context or is it at ha happen at the memory context?
It'll be in the memory context. It's always in the memory context. So you you can just do M map and read and write. So so that that's the way it works. So underlying it uses DAX and all the uh related support with that you can do it. Okay. Then uh in terms of provisioning, is there an application level visibility of how far this memory is?
Or is there like an SLA that you publish to the orchestrator application? Currently, there is no direct way for you to know how far the memory is left. But you create files. As soon as you start creating the files, then you get to know. When you pre-create the files in FAMFS, like for example, you want to say, I want to create a 10 GB database, and you create a 10 GB database. So you open a file.
and say I want to create a file with 10 g uh 10 GB size and that gets allocated. Okay. Thank you. For the uh famfs, just wondering if there are any security uh features that prevent like uh accidentally write uh like during the OS boot or some other initialization. Uh some'cause it's
Behind it, it's a sexy memory, then uh other applications might have access to it at the same time. Just wondering if there's any security features to prevent so though there is a tokenization uh which is being implemented right now. As of now, it's just that we are trying to evaluate it and then do it, but we're adding more and more features to it. So, yeah, so that's one point definitely is.
Being looked at, and then that that's what we will get to. Thank you, everyone. Thank you.
