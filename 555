Efficient_AI_Serving_at_Scale_Processing_Near_Memory_Acceleration_for_LLMs_and_Vector_Search

Okay, I think we should start. Hi, good afternoon. Thanks for joining. It's getting late in the day. So my name is Gorva Garwal. I'm a solutions architect at Marvell.
I'm joined with my colleagues Howard and Jajet. Together we'll be presenting energy efficient and cost effective solution for billion scale vector search which is centric to many AI workloads like generative AI and recommender systems.
But before we talk about the proof of concept that we have done and what our results are and what we have done, I want to address a question that I get typically asked. Why would you want to do compute near data?
Disaggregated computing in a heterogeneous architecture, it adds complexity. Why do I want to go down that path when I can perfectly do this in a non-heterogeneous, meaning homogeneous system?
So come to think of it, disaggregated computing is upon us in one form or the other. Take CPU, GPU, cooperative processing, coupled architectures that are fundamental to many data center workloads.
In that, CPUs perform the control flow and orchestration kind of operations, and GPUs perform highly parallel tasks like MLP processing. Because modern workloads, they have
Increasingly complex pipelines and with very diverse requirements. For example, take a mixture of experts, agentic AI or recommender systems. Those are very long and
Complex pipelines where disaggregated computing makes perfect sense. For example, pre-filled decode disaggregation for large language models, one of the very hot topics in the industry, especially ever since NVIDIA and
Announced dynamo, you would have heard about. Or you take recommender systems that are based on deep learning recommendation model where disaggregating the embedding tables due to their massive size and very low arithmetic intensity operations.
You don't want to perform them on your XPU. So, as you observe these heterogeneous systems, you will notice that the memory hierarchies are increasingly becoming longer.
So, what is the effect of that? Especially, traditionally, we are predominantly in compute-centric model. And when you follow the compute-centric model with long hierarchies of memories and caches,
Meaning, you're pulling data off of, you know, that's deep in your memory hierarchy all the way to compute, what's happening? You're incurring long latencies. So, latencies have been there forever. What's new now?
There are many things that are bad in these modern workloads. In that, for example, traditionally, how do we hide latencies? By smart caching and prefetching. But when your workload, I think somebody
Talked about being irregular memory accesses and with low arithmetic intensity, meaning you pull something, the instructions that would use the data that you have pulled are not going.
To be repeatedly using the data, so you're not amortizing the cost that you have incurred on latency across many instructions. So it's a total waste, it's a sheer overhead of pulling the data. That's just one consideration. And when your workload is
Data-centric, okay, it's happening all the time. And so, as a result, your compute is not being effectively utilized, you're paying penalty in latency, your solution cost is up, your performance is poor.
That's the main idea. I hope you got the gist of it. So, compute-centric approaches are inefficient for data-centric workloads. That's the point. So, instead, we want to have a data-centric model. So, that's the paradigm.
Shift, which is to do compute close to the data. And the idea is disaggregated computing with minimal data movement. So clearly, processing near data is a compelling option for data-centric workloads.
Let's look at a real life example of a product which is ready for deployment at hyperscale and is being deployed. So why don't you hear it from the architect of the product over to Howard?
Thank you. This is Structera A. I'm holding here. Structura A, where A is accelerator, a near-memory accelerator. So Structera A is a CXL device.
16 lanes of CXL 2.0 connecting to the host. It could be bifurcated into one or two heads. And the device has four DDR5 80-bit interfaces and the
The accelerator here is a complex of ARM cores, NeoVerse V2 ARM cores. The NeoVerse V2 ARM cores is a cluster of 16 cores with SMP coherency.
They run in excess of three gigahertz each. They're ARM V9 instruction set. These are SIMDM floating point units. They have RAS extensions, cryptographic extensions. They have a generous caching architecture, L2 and L1 caching, and the fabric has an.
LLC and they have the two generations of the scalable vector units in them. So these cluster of ARM cores are connected, interconnected with a mesh coherent.
Fabric that has many years in Marvell's servers. They're fully coherent, ECC protected, they run at 2.4 gigahertz, and it's a last-level cache of up to 64 megabytes of cache.
The same mesh connects to the CXL interface for CXL by 2, and it connects to the four channels of DDR5 with a bandwidth of 200 gigabytes per second. It's also got crypto capabilities.
So the memory can be cryptographically encoded. And there's also features like a DMA engine so that you can make local memory moves from traditional PCIe or from memory to memory using the core complex.
Just to give you some idea of the performance, so this can run with DDR56400 RDIMS or memory down, and you could see effective throughputs with the DRAM up to 140 gigabytes per second. If you hit in the LLC, it's over a terabyte of gigabytes.
Per second with extremely low latency, so you're hitting with 24 nanoseconds in your LLC, less than 100 nanoseconds to the memory. So that's structera A. Now we'll see a real application of it.
Hi everyone. Hello. Hi yeah. So uh now I will be talking about about uh one uh application of uh this hardware.
In a very important workload these days in the AI era, which is vector search. So vector search is basically the process where you try to find similar elements from a set of data elements using nearest neighbor algorithms such as KNN.
KNN is the basic one which doesn't scale. So we have approximate nearest neighbors algorithm which prunes down the set of data elements to search very fast and it's more efficient.
Trading off some performance, some accuracy. So, many use cases of vector search, including generative AI, rag workloads, recommender systems, computer vision, etc. So, vector search algorithms are
Generally, of two types. One is cluster-based and one is graph-based. Now, cluster-based algorithms have different characteristics. They are more suited for massively parallel processors like GPUs with thousands of cores. So, they come with the limitation of their.
That you need special costly hardware for that. But the most efficient and the most intuitive algorithm is graph-based algorithms of ANN searches, which are suited for CPUs and they provide more accuracy and just basically.
Very easy to adopt. So, this graph-based algorithms, the main algorithmic pattern in that is graph navigation. And this graph-based vector search algorithms are actually supported by many big companies.
Hyperscalers. They build libraries and frameworks for it, such as Meta's face, Google Scan, and we would focus on the face library from Meta for the rest of the talk. So, vector search, let's look at some of the key
Characteristics. So it's a predominantly memory bandwidth bound workload because of its random memory accesses, low, very little compute, and yeah, it's and the graph navigation pattern, it makes it memory bandwidth bound. So we look at, we see
A sample profile from phase vector search, we can see that the back and bound percentage in the top-down category of profiling is like the most. And yeah, so basically, and the operational intensity is so low, like 0.3. So, all the characteristics of a very memory bandwidth.
With bound workload. So let's see how the near-memory compute paradigm that God of just explained can help improve the performance of this workload. So let's assume that you have a bill.
Scale vector database, and you are running it off of CXL memory and processing on the host. So you have to so every pointer chasing that you do through the graph navigation, you have to read it off the CXL memory. So you end up reading a lot more data over the constraint CXL PCI bandwidth.
Interface. But if you offload the vector search operation to our Marvel structure accelerator, then you are no more locked within the PCI bandwidth and you can leverage the 200 gigabyte per second internal bandwidth of our PNM device. And you only have to be able to
To move very less amount of data over the CXL band with just the results and the arguments. So, this results like this results in a 60x lesser data movement. So, we study some metrics of how we can scale up the performance of vector search by adding.
Adding more structure to your host system. So we see that our device allows, unlocks this flexible scalability and allows linear scaling of your workload with the more number of structure that you add. So okay. Yeah.
Basically, adding more structure can scale up your performance by 2.5x. Now that we have looked into the just simple vector search kernel, let's zoom out a little bit and look into the entire ecosystem, like the vector database system.
So a vector database system is basically like any R D BMS system, but rather it sh rather it does a vector search rather apart from doing vector search, it has more functions like maintaining consistency, availability, durability, and all the fancy things that any product
Database system does. So that actually takes resources to do. That doesn't come for free. So, what we did was we did a program where we integrated the Milwaukee's vector database, a production-grade popular vector database with our structured IA accelerator, and we offloaded.
Just the search engine to our near-memory device. So, this is possible because of a plug-in-based architecture that Milvas provides that allows you to add more different kinds of hardware backends to that database. So, here we add, apart from GPU and
CPU, which is already available in Milverse, we had a Marvel Structura backend that operates over C Excel. We are showing this demo in our booth. If you want, we can talk more about that in booth B1. Finally, we did some preliminary results of scaling up our performance of Milverse using.
Structura, and we show that again similar performance scale-up, adding more structuras, linear scale-up that we find in the Milverst database. So we invite everyone to come join us if you like the use cases.
Sample these use cases, procure these cards from us, and let us know what you think your use cases, your specific use cases, if those make sense. And yeah, I think that's it. Yeah, thank you so much.
Thanks for the presentation. Can you please comment on what exactly in vector database search have you accelerated near memory? Is it vector to vector similarity search, vector distance calculations? And if so, what are the different
Equations or criteria of those distance calculations. Thank you. Yeah, so we have offloaded the entire vector search function, which is composed of three key components. One is obviously distance calculation between these thousands of vector dimensions, then is the
Graph navigation. So it's a multi-layered graph and uh then is uh sorting to cal keep the top K elements always ready. So yeah, we are doing all all all all three of those inside our uh accelerator. And we are leaving the rest of the database workload to the host. So the whole phase engine is drawing.
On structure as is the library that comes from, you know, the open source library that comes as is. Thank you so much.
