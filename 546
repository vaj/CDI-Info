Co_Designing_for_Scale_CXL_Based_Memory_Solution_for_Data_Centric_Workloads

Okay, good morning everyone, and thanks for attending. My name is Gaurava Garwal. I'm a solutions architect at Marvell, and I'm joined with this team. Anil Godbule, I work for Intel in the data center marketing group.
The CXL initiative. I'm also the marketing chair of the CXL Consortium. My name is Jimmy. I work with Alibaba Cloud. I'm working on a database group. My name is JP. I work at Ethcon Technology, many working on
On the connectivity switch, and we're excited to tell more about CXL, how to work on the AI workload. All right, so together we are presenting CXL-based solutions for a broad range of data-centric workloads, like serving AI, databases, KV-CAP.
caches, data analytics. So a lot of good stuff to cover. Let's jump right in. Okay. See if I can Okay, so modern workloads are increasingly increasingly data centric. Why?
Because of some key fundamentals like LLM models with billions and trillions of parameters, long sequences, very high-dimensional vector data, and so on and so forth. So, as a result, these workloads become memory-bound. And consequently,
The accelerator ends up spending time waiting for the data to be loaded instead of computation, which not only affects the performance but also drives the cost of the solution up. And moving tons of data consumes significant power too.
Ideally, there should be a large memory capacity tier that is available to the accelerators because the accelerators have very limited memory on board, which is typically HBM, and that memory, the memory is not going to go up in any way.
Time soon. So, ideally, what you want is you want a flexible system design so that you can compose a system that matches to your workload needs in an optimal way. Now, if we look at how can, you know, what are the different paths through
Which your accelerator can access memory today. Accelerator can access fire memory through either the scale-up memory semantic in a connect, but that's pretty much limited to accessing remote GPU memory, which again is not a capacity.
Here. Or it can tap into the CPU attached memory over PCIe. Now, when that happens, which is commonly done, but when that happens, what happens is that that memory is limited, and then when you have multiple access.
Hanging off of your CPU socket, that memory gets oversubscribed. And as a result, page faults, cache evictions, bad things. Consequently, it affects performance. Your performance becomes unpredictable.
And SLAs can't be met. So now what if there is a disaggregate a separate memory tier, which is a capacity memory tier, that's available dedicated to your accelerator?
And now you can alleviate all the pressure on the CPU attached memory. You leave that memory for the CPU work. And now your performance is predictable. But wait.
What if you could also have compute with that memory? Wouldn't that be a compelling option? Because having compute on that memory now brings, now it unlocks the full memory of your DDR that's available on your C Excel device. You're no longer
Limited by the CXL interface bandwidth. And also, as far as latency is concerned, any compute that you do near the memory, you're not paying anything extra as far as latency overhead of CXL interface is concerned. So here's an example.
Simple. These controllers have four DDR5 memory channels with 200 gigabytes per second of memory bandwidth per controller, all of which is available to the onboard 16 server class ARM Neoverse V2 cores. So the overall memory
Bandwidth available to the compute in the system is tripled to 2.4 terabytes per second in this example. And the compute horsepower has been doubled as well in terms of the number of cores available in the system to compute.
And the memory capacity has gone up from 2 terabytes to 10 terabytes. So you have flexibility to scale the compute and memory that fits your workload. Because, and since the modern workloads consume tons of memory,
Let's look at a real-world application that can make use of such scalability. That is in dire need. So, as you know, vector search is centric to many key AI applications. It requires tons of memory because vector data is highly dimensional.
Vector gets into many kilobytes, and then you have billions of these. Because vector database represents billions of items, and the challenge is to find a few items of interest in a very tight latency budget of a few milliseconds. So, how do you do that?
Are various techniques. When you look at the contemporary solutions, these solutions all have some compromise or the other, either on performance or accuracy or cost. One of the techniques that is
Proven to be very efficient is graph-based called HNSW. That's a result of 10 years of research. But the graph navigation doesn't fit accelerators very well because it's a deep pointer chasing kind of a problem.
And so we look at how this can be accelerated using the system we just saw, where you have CXL attached memory, along with it you have compute available for near memory compute processing. So what do you do today?
You deploy a scale out cluster with a bunch of nodes interconnected through some network. Now instead of that scale out, what if you were to replace those nodes with these CXL accelerators and you end up with s like a cluster in a box?
So, you are able to scale up your system in a cost and energy-effective way. Here's some performance that we see. In this example, you are able to scale the performance, the base performance.
Of the host by 2.5 times. Think rack scale. Your rack density effectively for the workload has become 2.5 times. Now, think about thermals, think about efficiency as far as network efficiency is concerned.
Even your, in a way, your copper reach has become doubled. In fact, more than that. So there's a big benefit. This is something that, one thing that we have seen is by doing near-memory compute, you are able to reduce the data movement significantly.
With this particular workload, you are able, what we see is instead of moving data over C Excel fabric to be able to do computation on the host CPU, because you're doing near-memory compute, you are able to reduce the data movement.
By 60 times. That's right. Not 60%, it's 60 times. All right. Let's look at the next workload. Anil's gonna walk us through. Hello. Thank you, Gaura.
For the introduction. Now, I'm going to speak more about CXL memory directly attached to the server. I think not this has not been deployed enough, so I'll come here and keep on preaching why it is so good. So, I'll begin by, let's see, listen. Okay.
I think the previous speaker Nauni talked about, but as we know, CXL is good, it adds more memory capacity, so in general, performance goes up. And of course, it can also add bandwidth. So, for those bandwidth-critical applications. But the real benefit is actually: while you do all that, while you get these benefits, you can also reduce.
The TCO. And the previous speaker spoke about that, and that's what I'm saying. Today, the 128-gig and 256-gig DIMMs are so overly priced, I mean, non-linearly priced compared to a 64-gig DIM. And I think the reason is because they have to use the higher capacity density DRAMs, which are
Probably not yielding so well. So, over time, that differential could go away, but at least we expect this to be around for this year and the next. So, the idea is simple, right? So, rather than to make up a capacity using these high-priced dims, you can use lower-cost DIMS like 64 or 96 and then.
Make up that same amount of memory using C Excel because now C Excel allows you to park more memory DIMS on the server. So, that is one thing I wanted to point out. And then, yes, even here, if you reuse your DDR4, and if the C Excel controller supports DDR4, then the TCOs, basically, a C Excel side.
Memories pretty much free, right, other than the controller cost. And by the way, the big cloud service provider CSPs at last year's CXL DevCon, they openly advocated doing that. Okay, so now before.
Before I describe the, now I'm going to go to how when you deploy C Excel memory, right? Traditional, like what are the techniques used? Because C Excel memory, of course, has higher latency. So this problem is not new. How to hide that latency has been done even before C Excel, ever since the dual socket CPU.
Came about. If a workload on the first socket has to get memory from the other sockets DRAM, Linux already implemented a thing called auto-NUMA. So it will do the hot and cold page telemetry and move the data from the other side into the local DRAM memory. So effectively the application
Application doesn't notice it. In the beginning, there will be a few higher latency fetches, but from there on, the page is in the near memory. And then C Excel, we do the same thing, right? So you do this hot and cold page telemetry, and then once they decide to move the page, migrate the page as they call it, then you stall the workload briefly and move.
The whole 4K page or whatever your page size is. Now, on the right side is what I'm going to talk about our CPUs since I'm from Intel, so I'm going to put a plug for the feature on the Intel Gen 6 Xeon CPUs and onwards called the flat memory mode. With this mode, we are essentially doing the same thing.
Doing the memory tiering as explained before. However, we do it all in the CPU's address by the addressing mechanism. So, basically, it is a hardware control way to do it. And we basically blend the whole CXL and system memory as one NUMA node, unlike the other case.
Where it is two distinct NUMA nodes. So, a lot of software, and I will show you some examples, actually gets a big benefit. Now, they do not have to worry about memory tiering and all that. And we do not depend on the OS because our hardware does this migration and we do it with lower latencies. We never stall the workload. So, overall, the performance is better.
If you use this hardware control mechanism, and I'll show you some examples. And then the best part is we are not dedicating any CPU course to do this hot end page telemetry, which I talked about when the software techniques use that, because that thing is not easy. Today, the page tables are so big that you have to dedicate.
Dedicate a whole CPU core sometimes to do this. So, here is a first example. We have been talking about that. I have shown this demo in the other trade shows in the past, but we were showing the SAP HANA running and using the, so basically, the workload actually degrades, of course, a little bit.
By up to 4 or 5 percent. So, this is not performance enhancement, but look at the TCO savings, right? So, 40 percent plus T CO of the memory TCO is what we are showing. And so much so that SAP and both Azure they have openly endorsed us at the recent SAP show. Please check out that link.
And so they are saying yes, they are going to deploy it, and that's the first CSP going to use this. And then internally, we also benchmark, like some other, like another big database, which is a MongoDB. And I'm showing an example. How the blue bar is the flat memory mode, and how we can.
And we come out ahead with the software-based techniques, both in terms of throughput and, of course, the latency is also lower. So please check out. I'm actually showing the demo, but I am single-source right now in the Innovation Village. But that demo is on my laptop. I'm going to go there and sit in the afternoon. So please come by if you want.
To see the real life thing. And then this is about, I don't think we have time, but this is CPUs can also leverage the CXL memory to increase the bandwidth. And this slide is showing that. And I wrote a paper, I guess I forgot to put the link on that with Micron: how this is software-based memory.
Memory interleaving. Essentially, it's a RAID-like example, right? So, how RAID SSDs stripe a file across, say, four SSDs, and now the file access time goes down by four times. We can do the same thing with the memory workload. Rather than put all the memory data in one DIM, you spread it across and you enjoy the higher.
Bandwidth. And then there are some examples of that. So all these vector databases and all the AI or other bandwidth-hungry workloads and HPC benefit from that. Okay, so that's my suite.
I guess we'll take questions later. Hello. Okay. I'm come here probably as a user of CXL. I'm with the Alibaba.
cloud uh database group. So we actually built a database uh uh similar very similar to uh Amazon Aurola. So we are actually the it's called Apollo DB. It's uh one of the largest uh actually database cloud database uh in China and uh
Asia probably. And we are kind of a very uh large user of all the new hardwares. Uh we are the first uh actually commercialized the uh uh Intel obtained. So we do a lot of research. We uh also uh very early we start the uh RDMA use.
In our database. So we try all kinds of new stuff at the beginning. And so CXL is something we're very interested in, and it's something we actually build at the first, I think the word first.
uh system with CXL switch. Okay. So it's not just the uh CXL memory, but it's uh come with uh uh CXL switch with uh uh coming from the XCON technology uh here in San Jose. Uh they also built the first uh switch and we're actually built into a system. Okay.
So, this is actually the kind of prototype system we built. It's very early this year, we actually tried it out. Now, we just released a rack built with actual CL switching it. So, it's more decent.
Or actually, a prototype. So this is a diagram of our system. Like we have two switch coming with seven host, seven host computer nodes, we call it, and a two switch, and they're a bunch of memory boxes. Okay, they are actually.
64 DIM in that system. So if we use 250 gigabytes memory stick, then that comes with 16 terabytes. Okay, so that's a pretty sizable memory. And this 16 terabytes memory are shared.
Seven hosts. Okay. So those hosts are Intel GNR hosts. So each with probably 192 virtual cores. So this is a very big system actually we can build into a rack. So with 6L, we come with two
Terabytes bandwidth, that's quite a big bandwidth. And then we run on this system. This is some like numbers you can look at. This is actually some experiment numbers, so you can compare how good the
Excel works. So we have the, of course, we have like a first two numbers and NUMA nodes, local memory, DRAM, access, latency, that's in nanoseconds. So remote nodes, that's in another NUMA nodes.
And we put on the sexel. And there's a sexile without switch, sexl with switch. So most of a lot of, there are a lot of experiments going on with sexl without a switch. So we are the first having a switch. Okay. So all those actually data.
Published in a paper called Unlock the Potential of CXL Memory on a Disaggregate System for a Cloud Native Database. It's published on the Sigmoid, which is a database conference. And actually, this paper also won the best paper of this year, 2020.
So I think a lot of people are very interested in sexl. So those are the actually the data, and we'll also measure with the different size of the sex cell memory. So the smaller the sexl memory, the the faster. So once become bigger into 16K,
That's like our database page, so-called page. So we move a lot of sixteen sized page. But uh that's become uh a little bit slower into the microseconds range. Um but uh there's a room I think we can improve the bottleneck probably in the
Not in the sexel, but in the CPU kind of memory cache. There's like a queue there, so it's kind of jammed up. So eventually we can work with Intel, we work with AMD, of course with Excon, we can improve the logic.
Size memory latency as well. But this is very interesting. So if you just look at it for smaller size, 164 bytes, it's in the range of new mass. So it's very nice. This is, of course, another
Another kind of latency comparison. Uh but that's what the Intel GNR system. Uh so we actually use this six L um memory and uh so uh for uh database you know there's uh like a buffer cache. So we actually put a uh move the whole buffer cache.
Okay, from the local memory to the remote CXL memory. Okay, so it's over the switch to a remote memory. So very surprisingly, you can see the performance doesn't even have any difference. So that means even if we put a whole buffer pool of the database,
Into C Excel memory over the switch, that doesn't make a difference on the database performance. Because the database has a lot of other overheads, which is larger than those kind of differences made by remote memory. So, this is very encouraging. So, we move the whole.
Buffer put into the remote memory. Okay, so this is another interesting graph showing how we deploy the database. So as a cloud database, each system, if it's a GNI system, it comes with 192.
virtual cores. Okay, so most of the user they on the cloud they use only eight cores or sixteen cores. So each of those a server can actually serve like twelve servers. Yeah, twelve instances.
A database instance on each server. So we can scale up. We put more servers on the system. So we look at a system that compared with RDMA because we used to have RDMA connecting all the
Uh uh storage and uh uh our compute nodes. So, uh actually come back. We are first actually do the computer and the storage separation. So, we actually separate the uh compute nodes and storage. Storage has a distributed uh uh storage system. Uh we have the compute nodes there. So um
Then they are all connected with RDMA right now. So we compare with RDMA because RDMA is another way that we do disaggregation. And if you scale up, you can see the red line is RDMA, the blue line is actually the CXL. And the CXL has a very good band.
Is can do directly memory load and store. So if I read 10 bytes, okay, it only reads 10 bytes. RDMA system is different because IDM system you have to bring the memory, this kind of memory from the remote to the local. And our page size.
Is 16K. So every time you read 10 bytes, you have to actually ship 16K memory over the network. So that's a kind of read amplification, right? So for the 6L, it's directly memory access. If you read 10 bytes, you just get 10 bytes. So with this
Read amplification, you can see the IDMA get the bottleneck right away, the network bandwidth hitting the uphead. So we only have can put three kind of three nodes, three servers on a system, and they hit a bottleneck.
Compared with that, SexL you can scale, you can put more database servers on it. The network is not a bottleneck. So that's, I think, it's a very clear benefit of having Sexcel over IDMA.
And the last two graphs, the second graph, are range select. Range select the first one is a point select, the second is range select. Range select is like you read a bunch of data together. So it's kind of better for large reads.
Because you read a bunch of data together. But still, you can see CXL perform a lot better than RDMA. And also, we have a system. Probably I should get a better graph.
Database have like a so-called multi-reads or multi-master system, which is very similar to Oracle Rack. You can have multiple database servers connect to a shared memory pool. Okay, so those memory actually are being shared between different nodes.
And there are conflicts between reads. So sometimes you have a read from one server, and then the other server will read it right away. So there are kind of ping-pong between different servers. So those are the performance showing that how this performs.
In this kind of systems. And you can see that CXL, although between YUDU, CXL 2.0 don't have memory coherent yet, but CXL 3.0 will have that support. So for now, we're
Have to make sure the consistency between different servers reading the same memory. With that, there's a lot come with it, so there's some overheads. But still, CXL is performed a lot better than RDMA.
So that's that's the uh another kind of system saying that uh we have how uh good um the sexl compare to RDMA because uh sexl is a lot cheaper um than the RDMA. Um
For the current, we are actually looking at how to get a GPU along with CPU, along with expanding the memory. So that's how we actually experimenting, connect a GPU onto the CPU. Actually, we are having a safe.
Running and it shows a lot of interesting benefits and a lot of performance benefits. That will be again published later this year on another Sigmoid paper.
Thank you. I'm getting I don't think we have uh time for Q A, but we would want to hear if you uh I I hope these workloads you find them convincing and if you have similar workloads or if you are an operator
And are there any inhibitors in taking this up? You have operational challenges for broad deployment. We want to hear that as well. And if you have cool use cases of your own, you want to come participate at CMS Working Group. Perhaps we can.
Do it together. And since we don't have time for QA, some of us will be available on the floor if you come by. Thank you.
