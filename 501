Characterizing NVIDIA Confidential Computing Overheads Across Model Inference & Training

Today, I'm going to be presenting about confidential computing overheads and how they impact performance on production, inference, and training workloads. I'm the co-founder of Tinfoil. I'm Tanya. We do verifiably private AI and We basically run all of our models inside secure enclaves, and so we are paying this tax on pretty much every inference request that we the inference token that we provide. And a number of our customers are performing post-training, and so they've also been very curious about how.

How these numbers line up. And then the reason that we decided to start doing this is because we'd seen a number of papers and we'd done a bunch of benchmarking ourselves, especially on Hopper. But for Blackwell, which introduces this, which basically Encrypts NV-Link, which is the connection between GPU to GPU. There weren't really any public benchmarks that we were able to find. And so that's kind of why we decided to do this. So, the key thing that I want you to remember is that in Confidential computing, or like an NVIDIA confidential computing, the main impact of all overheads stems from one root cause, which is that direct memory access is disabled between the CPU and GPU. And this is, it's also important to remember that this is a Point-in-time problem that will go away once inline encryption support makes it into the software ecosystem. So, you know, the Linux kernel, for instance, and that NVIDIA, CC, and Intel, TDX already support this in hardware. As a consequence of that,

It's also very useful to keep in mind that all GPU math and compute itself is free. It's only when you're going over an encrypted link, and in particular, the CPU to GPU, whether it is with data or commands, that you have to pay this tax. So, let's just kind of try to understand what is encrypted, what isn't. So, this is kind of like what a GPU architecture looks like. So, you've got two components. So, you've got the host, you've got the GPU. They're connected over PCIe, and then all GPUs.

Are connected to each other by NVLink. So, what is encrypted? The CPU's memory is, of course, encrypted. If you're familiar with things like Intel TDX, you know, the DRAM is encrypted. With NVIDIA CC, the PCI bus that connects the CPU and the GPU. Is also encrypted. And then on Blackwell and later generations, the links between GPUs, which is NVLink, is also encrypted. One thing to keep in mind here is that the HVM itself, which is the onboard GPU memory, is not encrypted, and it doesn't have to be because it is on.

On DAI, and so it's outside the threat model for confidential computing. So, where, so, how does data actually move between CPU and GPU? And so, this without CC, these transfers would just happen with DMA. Right, but under CC, as we saw earlier, GPU isn't allowed to directly reach into the CPU's encrypted memory, and so there's no DMA. And so, data has to kind of like the fix for that or the trick around that is to use the staging area that is visible to the host, and we Call this the bounce buffer, and like all data that is written to the bounce buffer itself is encrypted. So, you know, if you talk about the CPU to GPU side, the CPU is going to encrypt that, put it into the bounce buffer, and then GPU is going to read from that, and then and the GPU will decrypt it with GPU hardware, and then vice versa.

So let's try to understand really quickly how inference actually works before we start talking about kind of how this balance buffer actually impacts end-to-end inference and training. So, inference has two phases. First phase is pre-fill. And here you're reading, the model is reading the full prompt. And so there's a lot of compute happening, or there's, sorry, there's like a lot of data that is being read. There's not a lot of communication back and forth between the CPU and GPU. And then this particular Produces the first token. So prefill plus one decode phase essentially produces the first token. And then decode is more of like a step-by-step phase where you're having a lot of you're not computing on as much data because you're only trying to produce one token.

And there's basically a lot more like CPU to GPU communication as a fraction of the time spent on basically multiplying the matrices. And so these are kind of like the two metrics that we think about when we think about inference. And then training. Training is A little bit more like pre-fill in the sense that you're doing a lot of large matrix multiplies as opposed to something like decode, where there's much less computation happening per step. And then there's some synchronization that has to happen across different GPUs.

On how that's laid out, there's a number of different strategies to lay that out, and then you update the weights. And then the metric that we care about for training is throughput, not latency. And so, what are the CPU and GPU communicating about? Either commands or kernel launches. Or data copies, like host to device, device to host, that sort of thing. GPU doesn't just decide to do something on its own. It's driven by the CPU. The CPU tells it what to do. And during non-CC, the CPU writes all these commands into a queue that lives in the host memory.

Ring buffer that lives in the host memory, and this is called a push buffer. And so it'll write like you know, like a kernel instruction there. It'll reference some sort of like data if there is some data copy involved, and the GPU will DMA not just the data but also the command itself from the push buffer. Again, NC. There is no DMA, so everything has to go through the bounce buffer. And so, how does that end up impacting inference and training? So, in this talk, we're going to focus primarily on decode, and we're going to compare both CC naive or inference implementations that have not been.

Optimized for CC, or you can call it bounce buffer naive because a lot of these problems and overheads go away if you get rid of bounce buffers. And we'll compare that to CC optimized, so CC aware. And so the example we're going to be using for CC naive. Is VLM, which hasn't had any of these optimizations performed. And then for CCAware, we're going to use TRTLM, which NVIDIA has actually performed a couple different surgical optimizations that basically get rid of most of the overheads. And then we use a full confidential VM setup, we use Intel TDS.

Set up with NVIDIA CC on B300 and H200. And so for decode overhead, we see about 30% compute bound in the CC naive case. And so there's a couple things that you should keep in mind as you. Increase the concurrency, which means you're increasing essentially the batch size. You're doing more compute per step. That really reduces overheads. And you should also keep in mind that while these overheads might look pretty crazy, they're actually not that bad in terms of absolute numbers.

For instance, like you know, in the top line, which kind of looks like pretty scary with B300 KME TP8, you'll see that even for like one concurrency equals one, the actual amount of overhead is like just seven milliseconds. So that's useful to keep in context. And then a couple other things that we note here. So you'll notice that TP8, so TP8 means your model is distributed across eight GPUs, and then TP4 is your model is distributed across four. You'll see that TP4 actually performs a lot better than eight.

Especially in the low concurrency regimes, and this is because there's cross-communication happening between these GPUs. And the more communication you have, the more overhead you pay. So you want to keep that down. And then CC-aware engines definitely change the picture. It's like a very clear example here. So this is Gemma. On B300, you can see that with C equals 64, you're 19% with VLM, which has not been optimized, and with TRTLM, you're all the way down to 6%. And then similarly for Chemi, which is spread across.

8 GPUs, we're seeing 28% if you don't optimize at all. And this is a pretty crazy fact to keep in mind. You don't have to do anything to get inference to just work. And again, these numbers are in absolute terms, not that large. But still, with NVIDIA. Optimizations, you're down to 1%, which is really, really great. And then training, so here, a couple of things to keep in mind. Again, LoRa trains a very small fraction of the total model parameters, and full SFT trains all of them. And so you can see that.

That again, if you do more work, you have lower overheads. And we can definitely see that here. And then, also, another thing to keep in mind is that these were done with NVIDIA's Nemo training harness, and that was not optimized whatsoever. For CC. So these numbers still look pretty good. And then in terms of tracing the overhead, so like where do these overheads, like whatever the overhead is, where is it coming from? So what we notice is that kernel launches run about three to four times slower. And again, remember that these are micro.

Seconds in latency, so even though they may appear slower, they're still in absolute terms. Like you're adding 10 microseconds, which is again like not too much. With Hopper, they stay about the same. With Blackwell, there's like a regime shift as soon as you go from a single GPU to two or more. And so that's something that we want to work with NVIDIA to investigate. And then for like the thing that you should keep in mind here is that you should definitely try to use CUDA graphs as much as possible because kernel launches are again CPU to GPU communication.

CUDA graphs, you basically bunch all of these instructions into this one packet, and you just keep replaying it, and you don't have to make that many communications. The next thing to keep in mind is pin memory versus pageable memory. So, pin memory is a consequence of direct DMA and is. Widely used for all inference. You can't do that again under CC, so you have to go through this bounce buffer. The kind of like a lot of inference engines optimize for the pinned memory path, and that becomes actually slower under CC. Like it's actually better to just use pageable memory in most scenarios.

So that's basically like a small optimization that, if you keep in mind, you will see significantly improved performance. And then between GPUs themselves, another really cool fact is that NVIDIA adding NV-Link encryption between GPUs. Used barely impacted the bandwidth. It is extremely fast, and it's easy to see that because in Hopper, which doesn't have NV-Link encryption, you're seeing, you're getting basically the same bandwidth as on B300. And so there is some cost.

Here, which is like the kernel launch cost, potentially. This is something that is like, you know, again, something to dig deeper into. But yeah, these are some amazing results. And then when does that again happen? So, for instance, this is like, you know, like a VLM like reference, like inference. Recipe, and you can see that there's like a number of strategies here. So, you know, there's tensor parallel, expert parallel, data parallel. There's like also pipeline parallel. And these are just strategies by way of which there is communication happens across GPUs and like how the model is laid out and how like data.

Is laid out there. So, another thing here to keep in mind is that CC has to disable NVLink multicast because NVSwitch, which basically allows you to do some computation inside the switch itself. Is outside the trust boundary, and so it can't operate on plain text anymore. So, a number of kernels, and this is another optimization that NVIDIA did in TRTLUM, which is a lot of kernels are written, may not even use multicast that much. Maybe there's like one operation in like, you know, like a hundred.

Like hundreds of operations that is using multicast. But if you use a CC naive engine, it will just completely disable that entire kernel, even though all you needed to do was switch out that one piece, and then all the other optimizations could still continue. So, how do we actually reduce this? The biggest thing to keep in mind is that you want to be batching as aggressively as possible, because you want to be doing more compute as a fraction of the amount of communication that you do. And then another thing that's useful is if you can do something with a single GP or you can reduce the number of like Or you could increase the number of replicas, but reduce how they're split. So, you know, if you do a four-way split and has served two replicas versus doing like an eight-way split and serving one replica, you'll see better performance with the lower splits. Always use CUDA graphs, skip pin memory, use pageable memory.

Memory. Another interesting fact that we discovered was speculative decoding. Like, you should reprofile how it behaves in your case because it does multiply the taxed crossings, the CPU to GPU communication. And so sometimes it might yield slightly lower performance than you were maybe expecting. From the headline results, but again, some of this can be optimized and worked on, and then rewrite kernels so they're not using multicast while allowing you to keep the rest of the kernels gains. And then some final few takeaways. While BounceBuffer is responsible, this isn't.

Necessarily something that NVIDIA themselves can fix alone. The hardware already includes support for this. And so, what we really need is for the rest of the ecosystem, such as the Linux kernel, to include support for this. In the meantime, if all you want to do is run models privately using state-of-the-art inference, you don't Really, we have to make any CC-specific optimizations. That's kind of what we've been doing at Tinfoil for like over the last year. We've barely made any optimizations so far, and it's been pretty good. However, like once you start approaching writing your own custom inference code, Need to eke out, like, you know, like the last bits of performance gains, then you may need to make a few surgical optimizations. But another thing to keep in mind is while NVIDIA is already made like TRT, LLM, CC aware, they're also contributing to SG-Lang and VLLM. And a lot of these performance optimizations don't have to be made recognizable.

Regularly. It's not quite just a one-time thing, but it's not something you have to do on a daily basis. And yeah, that's a bit about me. And yeah, feel free to read the full post. Thank you.
