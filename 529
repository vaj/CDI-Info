Overview_of_Linux_Page_Migration_and_Accelerating_It_With_Multi-threading_and_DMA_Of..._Shivank_Garg

My name is Shivangar. I am a Linux kernel developer at AMD. So today I'll be presenting, I'll be giving an overview of Linux page migration and how did we accelerate it with multithreading and DMA offloading. So first we'll cover a little bit of background on page migration.
It's important and how it's performed, then limitations of current implementations, and our proposed enhancements, then some results and future works.
So, let's take a brief introduction on NUMA systems. So, on NUMA systems, non-uniform memory access, we have fast local access to memory on its local node and slower remote access on memory on different nodes.
In these modern memory systems, it's becoming much more complex with introduction, with evolving hierarchies like CXL, HBM, and GPU nodes, et cetera. So, let's see what is page migration.
Page migration refers to physical movement of memory pages to different region or node while a process is active. And virtual address is virtual address used by process remain unchanged. So let's say this is the process. So currently it's mapped to node 0.
So i if we migrate from node zero to node one, see its uh virtual address remains same and now it it is mapped to node one and it all happens transparent transparently such that uh process remains active. So this is the kernel's magic trick. So why it's needed?
So it uh page migration is a important subsystem in kernel and it supports uh other features like NUMA balancing, memory tiering, uh, page promotion demotion, then compaction, hardware maintenance and user driven migration.
Let's see each one of them. So in MUMA balancing, we move memory closer to the task of closer to the task accessing it. For example, this socket the task is scheduled on socket one, but memory is on node zero. So
Each access is remote and slow. So, using page migration, we move it to node one. And now it's local, so it's fast. Then memory tiering. So, we have uh fast memory, for example, uh DRAM connected to uh socket.
And then a slow memory example, uh C Excel, where we can attach uh cheaper memory. So uh it may happen that uh our pages are there on high capacity, slower memory. So we may want to promote it to fast memory for efficient use.
Users. Or so uh yeah. So now uh after page migration, the memory is promoted to fast memory and uh app the user application will run much faster after that. Then uh page migration.
Migration also helps us address fragmentation issues in the system to make system for higher, to make space for higher-order allocations. For example, in this, the physical node is very fragmented and there is no room for large spaces, large pages. So our memory compaction.
Demon will compact the system, compact the physical pages to one location so that we have some contiguous chunk of memory where we can allocate higher order locations. For example, 2MB pages, THP. Then it also helps us address
Any possibility of memory failure. So when uh when there is like uh it is suspected that memory can fail, uh there is some there could be some memory failure. Uh but the page is not corrupted yet. So we would migrate the pages to a different location. Uh
And then uh we've and we can soft offline those suspected reasons uh where there's chance of failure. And uh so similarly for uh hot unplug. Then uh user-driven migrations. So it allows user tools of workload.
To control memory placement from user space to optimize memory placement in UMA system. So a user can use system calls like move pages, migrate pages, and mbind to convey such information. Now we saw what and why. Let's see how
How? So, first we start with a list of folios. Then, this list of folios is isolated. It means like remove the folios from the LRU list and then add them to migration list. So, we perform
Those uh then we process the list in batches of up to n R max NRMX batch migration. So, this whole page migration is a multi-phase process and it it happens in batches. So, now we have a batch of some folios. First we all uh
We unmap the page. We go into Nmap stage. So, in Nmap stage, we allocate a destination folio on target node where we want to allocate, where we want to migrate the page. Then we replace source folio page table entries with the migration page table entries. So, migration page table.
Entries is like a kind of a swap entries where it will tell the user, like if a user falls on the page, it will tell the user to wait. Then we do a single efficient TLB flush. So this happens for
For all the unmapped folios. Then we come to third stage where it is in the move stage where we migrate mappings and flags and then copy content and then update metadata, LRV list, ref counts, and restore working PTEs.
So, this all happens in the move stage. It all happens in serially, one by one. So, this is the bottleneck we are trying to solve. And then, cleanup where we address any error handling or any page that was not migrated. So, we fix that.
So, now till now we saw what is page migration, why it's so critical in our performance. Now, see the problem and our proposed solution. So, in memory.
Migration, we have the memory migration subsystem is very critical because it directly affects the application performance. And we have high page migration costs incurred in large-scale tiered memory systems where frequent page promotion demotion can occur, or CPU-GPU coherent system where GPU is.
CPU memory is exposed as NUMA nodes. So we try to profile the move pages system call to understand the overheads of folio copy. So we saw that in 4KP4, we have a lot of functionality
Folios, 22% of folios, 22% time was taken in folio copy, while 78% time was taken in other operation. And for 2MB folios, this was 97% of overheads were folio copy overheads and 3% overheads from other operations.
So this shows that there is some we can optimize this folio copy part to improve the performance of page migration. So modern systems are equipped with powerful hardware accelerator and high CPU core counts.
And in current memory migration implementation, we use single-threaded CPU copy. So it underutilizes memory bandwidth, and our ideal CPU cores are not utilized, and hardware acceleration potential remains untapped.
So, we observe that the migration system is moving toward batching. So, first we batched migration operation, migrate pages system call to take some number of folios to do the operation. Then, we batched TLB flush operation. So, instead of doing TLB flush after the page,
After every page migration, we do we do it in batch because TLB flesh is costly. Then the logical next step is to batch folio content copying.
So, yeah, so this takes us to the batching folio copy operation. So, in current design, for each folio, first we copy folio metadata like flags and mappings, and then copy folio content from source to destination, and then update page table entry with new mappings. So, this all is done.
Done one by one, serially. And this is the main reason why memory bandwidth is underutilized and modern hardware and we can't take any benefit of modern hardware. So, in our proposed design, we decouple the folio copy operation into a separate
part. So now for uh now we first do in batch uh copy all the folio metadata and uh flags and mappings. Then we uh copy the folio content in batch and then the rest of the part like updating PageTable entry with the new mappings.
So, this batched copy makes it easier for parallelization and hardware offloading. So, now we have batched the folios. So, this takes us to how we can make it more efficient. So, we use multi-threaded copying with CPU. So, in this
We divide the folios. For example, we have now 512 folios. So we divide it, we divide it into chunks and then distribute it across multiple CPU threads. And then we use work queues to copy data to the target node. And so large folios get more copy through.
boost because of the design of because we have less metadata and kernel overheads. But with this approach, this is the open question like how we can do resource accounting and ensure fairness. Like who will bear the cost of page migration?
Now, we take one step further to offload the folio copy part to DMA hardware. So, in this, we dynamically allocate DMA channels, and then, like we, like normal DMA operation, we
Map the source and destination folios, prepare DMA descriptor with completion callback, and then submit the transfer equal transfers across channel in round-robin fashions. For example, we can allocate 8, 10, 8, or 16 channels and then distribute our folios to diff on those channels.
And then issue pending request and then wait for completion callback. So, this helps us parallelize our folio copy and offload it to hardware. And if there is an error from DMA, if hardware returns error, we will fall back to CPU to ensure.
Successful copying. So, but DMA comes with its own challenges, like DMA is high setup cost. So, we need to balance, we need to find a sweet spot where DMA setup costs are less than the benefit it gives.
So, with this approach, we benchmarked move pages, we did a micro-benchmark and checked the results for 4KP folios. So, 4KP folios didn't show much improvement because of high condal meter overheads, because there are like so in
Multi-threaded CPU case, it's like even with multiple threads copying the data, we didn't see much improvement. And for DMA offloading, performance degraded. This is because there are like high setup overheads, and then we benchmarked two MB pages.
So, for 2MB pages, we see a good performance improvement. For example, yeah, so here with 8 threads, we got up to like 3.5x times throughput improvement using CPUs. And this is the baseline. Baseline is native.
Single thread CPU copying on the generic kernel. So and for with DMA offloading, we got a 5.16 times throughput improvement. So 2MB pages, we saw a good benefit with 2MB pages because there are less overheads to manage.
This metadata. For example, there are 5-12 times less page table entries and then less effort to manage the list and all. So then there are open questions. So this is still in RFC. So then we have some open questions about.
Performance, like when this we will like solve with more experiments and discussion with the community, like when we will select DMA and CPU, and how do we schedule folio copy worker threads across different platforms.
In some platforms, for example, in AMD, Epic, we have CCD and CCX. So we need to efficiently schedule across those. And then do we need non-temporal instruction for CPU-based mem copies? And how do we upscale or downscale worker threads depending on various factors like system?
Users and all. Then we have to standardize offload copy APIs. And we have to think about how to amortize DMA cost. And then think of resource accounting, like who will be at the migration cost, and is it fair. And in future,
We plan to test DMA offloading on SDXI. So, SDXI is a new age smart data stream accelerator. So, it's much more powerful and it has many features. One of them is data offloading. So, we think we will get more benefit.
Benefits with SDXI. And then address open questions and work toward upstreaming. So here are links and references to the patch set where you can look into the code. I didn't go into a lot of details because of limited time.
But here you can explore more and you can ping me if there is any feedback. Thank you. So any questions from audience?

Uh you are talking about this like slow memory path okay uh yeah slow memory.
Uh what is the reality basic compass and then it's slow? Um so like uh so when we attach memory, so this uh slower memory, it's attached to P C L E uh P C I uh socket. So and they're not going to be able to do it.
Where it shows CPU less NUMA node, and it's called CXL node, compute express link node. So that's like recent development in hardware. Yeah, it should.
Shows up as a NUMA node, and we can use tools like NUMA CTL to get the distance of those NUMA nodes. I had a question about over here. I had a question about the
Multi-threaded copies? Yeah. I'm assuming your page migration happens from one NUMA node to another NUMA node, right? So my idea of how pages move between NUMA nodes is through a PCIe Express bus. So how exactly does
Does multithreading help there? So we need, I didn't get your question, I think. My understanding of how pages move from one NUMA node to another is that there's a PCI Express bus where when the kernel requests that this page is. No, it's through, like, so different NUMA nodes are connected to.
Through this NUMA interconnect. For example, in this diagram, we can see. So, if we have to move the data, so we can just copy data from this node to that node using some, like we can use simple mm copy, and then CPU will.
Do the copy from one node to another. So it's going through NUMA interconnect. Let's just say CPU 0 is connected to node 0. Yeah. CPU 15 is connected to node 1. Yeah. So if a page should be moved from node 0 to node 1, my understanding.
There's a PCI Express bus between the memories itself which does that? Uh no, it's not PCI Express bus. So uh if we use CPU, CPU will do it. And uh if we if the memory is on C Excel node, then it will go through PCI, but then it's still uh CPU is doing it. Uh
If we use hardware offloading, like DMA, we will have to set up the DMA, do all those DMA mappings, then DMA hardware will do it. So I didn't get what you mean by PACI is. So Node 0 loads the page into the CPU 0.
CPU zero communicates it with CPU fifteen. Okay. And CPU fifteen uh pushes it into node one. Is that the process? Uh no, a single CPU will do the task. It uh it will not uh sorry, I uh I didn't get your question. We can connect afterwards. Uh also one more thing about can you go to the unmap slide? Okay.
So, first you unmap the page, right? So, if there's a request, if there's a if someone tries to access the page after it's unmapped, how would you handle that kind of situation? Yeah, so first we log the page, and then we so and uh
To ensure that there is no further access, we replace the source folio page table entries with the migration page table entries. So it's handled like similar to if a page is swept into swap nodes. So it will like OS will ask the OS will
If uh and if uh a user process is requesting for that ti uh that page which is under migration, it will make it wait. So and it it all happens transparently, so uh the user or process will not know about it.
Yeah, you also spoke about CXL being one of the connectors for your case.
You also explored on having an accelerator on the CXL side and using that accelerator to do your praise migrations? So, you were asking about if I did any experiment with C Excel node? Correct. So, in your diagram, you did mention about being connected to C Excel. And C Excel is treated as in slow memory. Yeah, you are talking about this, right? Yeah, correct.
You can also have an accelerator on the C Excel side, which will very well help you in doing your page migration from C Excel node to your fast memory node, or vice versa, right? Yeah. Instead of your CPM doing it, you can offload the same to the accelerator on the C Excel side. You're saying if I use a hardware accelerator for this? Yes, exactly.
So, actually, I didn't do any experiment with CXL because there were some setup issues on my system with CXL. So, I did normal experiments with DRAM nodes. Yeah, so it's in my plan to do some experiment with C Excel. And I believe it will show more benefits because
Because uh transferring data from C Excel is little slow, so hardware accelerator will help on it. Yeah. Thank you.
Uh what's a T L B and what's its purpose? T L B T L B is a translation looker side buffer uh and then uh it's like a cache.
Where you can store virtual address to physical address mappings. So it's like you don't have to do this whole page table walk. Like I'm saying a lot of OS terms. So this page table walk is very costly operation. And if we use this.
TLB, we get to know this physical address very fast. Yeah, so. Okay. So, and we do, like I mentioned about TLB flushing. So, this TLB has some entries about virtual address to physical address mappings. So, if we like now.
Now, since we are migrating the page, we are moving its physi location from one physical reason to different physical reasons. So these TLB need to be flushed. Otherwise, it will point us to a wrong location. So to get a consistent view, we do a TLB flush.
So, and one point I missed. So, I talked, like, I interchangeably used folios and pages in my presentation.
So, Folios is a newer concept in Linux MM. So, it's just a contiguous chunk of memory, and it is handled as a single unit. So, it has some less overheads. Yeah. Hello.
First, I'll do some shameless advertisement for my talk. If you want to know what is a TLB, you can attend my talk. It is at 1 o'clock. Linux is leap beyond fixed pages. So, my question is: Hi, Shivank, I'm there. We have interacted on the list, right? So, I was looking at version 2. I think RFC is at version 2, right? Yeah, correct.
There weren't any comments. Are you receiving any pushback, or what is the status? So, yeah, I'm working on those comments. So, like, what's your question? Like, in V2, I saw there weren't any comments. I think you posted it some weeks back. Yeah, I posted it in around.
Around March before registration of OSS. Yeah, and yeah, so it's still in, so we got some comments from community folks in LS FMM. Alright. Yeah, so I'm working in them. And then ZN from Nvidia is also looking into it.
Different use case. Okay, okay. Thank you.
You spoke about the challenge with respect to accounting. Can you brief what's the challenge there? Is it about which tasks you want to account to, or is it what's the challenge there? Yes, so like now we are using kernel threads to move the data.
So it's like now the migration cost will accounted on kernel part. So there is a challenge like how to ensure fairness. For example, there could be different cases where memory is migrated and kernel.
Kernel is bearing the cost. So this could be unfair to other processes. So this is. In other cases, let's say even if you don't do that, still kernel has to do the job, right? It's still part of the kernel accounting, should be known. You may have to do it in behalf of which process.
Process, you may be some process, let's say one process is doing this in a continuous manner. Is that the challenge? Like, you want to be fair among the process, or? Yeah, like I have to think about that. So, so on this thing, multi-threading, multi-threaded.
Copying with CPU. So ZN is working on that, worked on that. So it's for like a different use case. So I have not looked into it in detail. I worked on this optimization three hardware offloading use case. So I have to look into how
This fairness challenges come uh like all right.
Hi. To add to the same comment, right? So, when the migration happens, take an example that you have a socket which gets allocated by a task. But when the task moves from one group to another C group as such, the memory allocated for the socket would
Just remain in the initial group as such. That accounting is still a little complex when it comes to migration as such. So I sorry, I like there is little echo in this room. So I like it's difficult. I was just trying to answer here. When he asked for accounting about the
Memory model, right? So, what happens is, like, consider this example. So, everything is being C groups. So, a task gets, task allocates a socket or a socket connection happens, and the task migrates to another cgroup as such. So, what happens is the initial allocation of stock and the socket memory, right, that stays with the initial group. So, what about that kind of thing?
Kinds of issues are still there with accounting when it comes to just adding permit to it. Okay, yeah, so you're talking C groups will handle some part here. No, the accounting goes there. So it's still a problem when you do a migration. Yeah.
Hello. So major bottleneck is coming from the batch copy you said, right? I mean, other than TLB and replacing the mappings. So why is it that
You get major thoroughput improvement with only two MD pages, I mean, with large folios. Can you explain more on that? Yes. And moreover, on DMA setup, I didn't get that part. Who is going to be that DMA engine who is going to do this batch copying operation?
On the first part, yeah, so why didn't we see any performance benefits with 4K B pages as compared to 2 MB pages? So 4 K B pages, so like as compared to 2 MB pages, like we have in one 2 MB P M D entry, we will have 512
4 KB pages and the overheads to manage those date those 512 entries, like there will and then we in those lists that cost is exceeding our any benefit. So we didn't see any benefit with the
4 KB pages. So, because there is a lot of kernel metadata overheads of managing those 512 entries as compared to one large folios. And then with large folio, the other setup cost will be amortized. So it will be distributed over, like it will be divided by 512.
So, we will see more benefit there. Okay. So, maybe other part that DMA setup you were talking about, I didn't get exactly that part. Who exactly is going to do that DMA? I mean, in sense edge copying. So, for that, like.
It will be managed by the kernel. So, we created this migration offload infrastructure. So, we can ask the user to register this offload driver. And then we can
Some other subsystem like using sysfs interface, we can initialize this offloading driver, and there it will do this stuff. No, no, I thought Mike.
Question Vaski. So doing this batching operation, batch copy operation, which is the major time taker. So rather than doing it on CPU, you said that it would be, you would do some DMA setup and offload this to hardware. What is that hardware which would do this batch copying?
Is that hardware? So, so in every system, we have like this DMA hardware. So, it's used by many other subsystems. And then, so I'm targeting on data server chips. So, they come with the special hardware. For example, in AMD Zen 3, we have this PT DMA.
Hardware. So that will do that DMA copying, hardware offloading. And in newer generation, SDXI is coming. So that will manage it. Okay, and what are the major use cases that would benefit? I understand that hard pages you want to keep.
With the CPU which is executing and accessing that. But I mean, can you elaborate more on the use cases that would get benefited? And other than data servers, I mean, other use cases if you have an idea where this will benefit? Yeah, so there are use cases.
Is for example in high performance computing and now these LLMs are coming. So they have like huge, huge memory. So we need to distribute that memory on different NUMA nodes. So it may happen that workload is running on one socket and its memory is.
On other socket, other remote NUMA node. So we may want to move that memory to the local node. And then C Excel this is also a very new concept in this. And CXL is like we can attach a cheaper memory because memory is very costly.
So we have this cheaper memory we can attach to C Excel and earlier there was like some concept of N V M M, non-volatile main memory. So we can attach those cheaper memory to the C Excel sockets or PCI bus.
So there, like, we can have some cheaper data centers. And then a memory failure is also an important use case because it may happen that some reason of some particular node is failing. So, if our OS suspects,
Expects that this is going to fail. So we can just migrate our data to different regions so that we don't face any data corruption. Okay, thank you. For the DMA offloading, you are actually submitting.
The request and then waiting for the completion callback, right? And that's synchronous. But if you are doing synchronous with DMA, which means that you are not going to reap the full benefits of DMA. And I don't know if it's possible to do asynchronous DMA with your use case, but it's something you should explore. Otherwise, using DMA wouldn't give you
You have much benefit over CPU copy? Yeah, I agree with that. I was looking into that, but with asynchronous, I was facing a few challenges. Because we have to return those pages to the memory management again, MM subsystem. So if we do asynchronously, then
Like somewhere we need to have that stop. So, okay. Yeah, I agree. I'll look more into it to optimize performance. So, as you can see, my data, I'm getting five times throughput increase with 16 channels. So, it's not that scaling, it's not scaling properly.
So, we need to put some effort there to improve the scaling, and it will further increase the performance. And then you also need to do it only for high, I mean, more than one megabyte of pages, because if you do it for 4 kbp,
Pages, that is an overhead associated with DMA. Yeah, so yeah, so we are targeting higher order pages. And so many workloads benefit from higher order pages. So that would be the primary use case for this DMA offloading. Thank you.
Hi, Shivanj. This is Archana. Good talk. I just wanted to learn about if you have explored hardware multithreading, and I just wanted to know how it fares out in regards to performance in page migration. What do you mean by hardware multithreading?
It's a hardware technology and like multithreading is enabled on the hardware. So you have like two threads running. Anybody can explain it better. Okay, so it's like simple multithreading. We do software multi-threading, and then thread is scheduled on multiple CPUs.
Are you talking about that? No, so this is different. You can just enable multithreading, hardware multithreading, and you can just check your performance with that. Okay, okay, sure. I'll look into it. Thank you.
There is one question here. So, hi Shwansh, good talk. So, how do you explain this decline in performance?
When you increase the number of cores from 8 to 16, as in, is there any explanation or yeah, that's an interesting question. So, yeah, we did some experiment on that. So, in our like this was performed on AMD Zen 3. So, we have this, like, every socket.
Has this four CCDs. So, like, yeah, so when we are scheduling up to eight threads, so we get this performance benefit. But after eight threads, we are reusing those C C D. So, our bandwidth is.
Saturating. So that's why we were not seeing any performance benefit after eight threads. Yeah, so this was this I meant by mentioning platform-specific scheduling because due to this AMD architecture, we have this CPUs are divided into
This different CCD, and each has its own bandwidth limit. So, yeah, that's the thing. So, the main point is: like, we need to distribute.
Or we need to spread those worker threads across different CCDs. So, this was the main crux of platform-specific scheduling. So, I think we can take any further.
The question in the corridors. Thank you.