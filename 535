KNOD_ In-Kernel Network Offload Device for GPU-Accelerated Packet Processing - T. Yoo & H. Lee

Hi, everyone. Today we'll introduce a K-naut, an internal network offload device that runs XCP packet processing on a commodity GPU. So I'm Ho Yun Li, working as a BPF system engineer at SUSE. And this is my colleague Tehi Yu sitting in the front, working as an algorithm.
Engineer at the Line Investment Technology. And alright, now let's take a look at our project Knaught. So before we get into Knaught, let's first look at the workload. So here we have a 100-gigabit link with a minimum size Ethernet frames. So the minimum size
Size of the Ethernet frame is 64 bytes, and including with the physical layer, it will become an 84 bytes. So, if we calculate the packet rate, it will get about 148.8 million packet per second. So, it's like a huge number. And so, how do we handle a packet rate high like this on the host?
Well, the usual answer is to add more CPU cores. Simple. This is the graph from the XV paper on the right, and this shows the XTV throughput scales quite well as we add more cores. That's a good news. But those CPU cores are not free. So every core we
Used for the packet processing, it's one less for the application. So, the real issue is not the bandwidth itself, it's more with the host CPU cost under with this 148 packet rate. So, how is this handled today? So, well, there are four kinds of broad options.
So, the first one is the most common. Just use a regular NIC and the host CPU. So, just NIC receives the traffic, and the server CPU does the packet processing. It is pretty simple and inexpensive. And then it is quite common in startup and a SaaS-based environment. And there is a second option.
Which is a SOC-based SmartNIC, or using a data processing unit. So, with these kind of NICs, part of the packet processing could be offloaded to the card itself. So, I brought an example with the Natural Agile card show here. It does the XDP offload to the card.
So this kind of option is pretty common within the enterprise environments. And the third option is using an FPGA-based smart deck or the accelerator. So with here the packet pipeline can be implemented in a programmable array. So that will allow us to move more packet processing logic onto the core.
So, it is powerful, but the hardware and the development are a bit more specialized and tricky. So, this is why we often see this option at the cloud providers or the telcos. And for the last, we have dedicated appliances and ASICs. So, here the packet pipeline is built directly into the hardware.
So, the performance is very excellent, but these are super expensive and highly specialized. So, that usually makes sense only at a hyperscaler or so. So, now we put this in a four option together, you could see the pattern. So, to the lower left,
The option is much more affordable, but the host CPU does most of the work. As we move up to the right, upper, the performance improves and the host CPU loads falls, but it becomes more and more expensive. So the missing part that we are trying to focus on is the lower right area.
Which will be here. So, this is where our Know project is positioning. So, using a commodity NIC and plus the commodity GPU, we are trying to get useful packet throughput with the less CPU host, and you don't have to buy anything.
Kind of a specialized NIC or the accelerator. So, what does that look like in practice? So, let's start with the cost. So, our development setup is pretty cheap. Uses the Radion RX6C600 plus the Connect X5 NIC. So, you could get these pairs on the secondary market about like $450.
And you can see the FTGA option is cost of about more than 10 times. And the dedicated appliances will cost like 100 times more. So, even with our development setup, it is pretty cheap compared to these options. So, let's talk about the performance. So, one thing to keep in mind before I talk about the performance is
Knaught project is still at a very early RFC stage. But even so, the current prototype reaches the 70 million packet per second. So it is promising. So if you look at the left graph, look at the CPU utilization comparison. The left bar is the CPU-only pass and the right bar is the K0.
And with the CPU only case, you could see the red bar, which is the portion that BPF program runs on the CPU host. So by using with the KNA, we could offload all of this VPN workflow to the GPU itself. So you could earn the CPU gap, you could use it to the application.
Or anything you want. So, even with a prototype, it's a very promising sign. So, the concept of packet processing with the GPU itself is actually not new. So, the project that I brought in here is an L2 for NVIDIA project, which is just an example using a DPDK and a
CUDA for the packet processing. So NICRX will be written into the user space memory, which is located at the DPDK mempool. And the application will dispatch the CUDA kernel to process the packet. So it works, but the developer has to work on both sides: the DPDK and the CUDA.
So, what's the difference with the Knot? So, our Knot project provides an XDP and a BPF for a programme and model. So, developers can just write an XDP program. So, and the Knot will handle all the rest. So, it will compile the JIT with the bytecode to the GPU, and it will offload to the GIP.
GPU by itself. So you just only have to care about the XAP, no other GPU programming or other stuff, doesn't have to care about it. So if you have an XTP program already running on the CPU host, you could just also offload that XP program to the Knoll. Simple. So now let's look
At how Knot's architectures are. So, this diagram shows the overall Knot architecture. So, at the high level, it has two main parts: which is the control plane, and the second one is the data plane. So, first, with the control plane, it actually prepares two things before packet processing. So, first, it has a
Bind stage, which prepares the memory path between the NIC and the GPU, and the second is a JIT stage that turns the XCP program into GPU ISC. So, and we have a data plane for the packet processing. Also, it has two stages, which the first one is a dispatch stage, which will gather new packets and also.
The packet processing to work with the GPU. And the second one is a vertex stage, which applies the result for each packet, such as XCP pass, XCPTX, XCP drop. So let's start with the control plane. So the first control plane job is a bind which connects the NIC receive path to
The GPU back memory. So before we look at how Kena does that, let's start with the conventional RX path. So normally when an IC initializes, the driver prepares a page poll in the host memory and they post those stages to the RXQ. And as packets arrive,
The NIC DMA will write the packet data into the past pool-backed RX buffers. So, incoming data will just flow to the host memory. So, once the RX work is ready, the NIC interp schedules the NAPI, which is a new API, and NAPI then pulls the RXQ and processes.
Multiple packets. So, this is the conventional Rx path. So, now suppose we put a GPU processing on the top of this conventional path. So, the packet has already rented in the host memory, right? And after Nappy posts the completed RX descriptor, the packet still has to be copied to GPU memory. So,
I had drawn the diagram here. So, actually, the packet crosses two PCIs. So, from first the NIC to the host memory and the host memory to the GPU. And the packet rates that we are talking about, like 148 million packets per second, the second
PCI transfer consumes a PCI bandwidth and it adds a latency. So we need a direct RX path. So this is what Knot changes. So instead of using the backing the RXQ with the host memory pages, Knot backs with the GPU memory directly. So then NIC can just directly DMA write to GPS.
The GPU-backed pages over P2P DMA. So under the hood, it exports the GPU memory as a DMA buff and just bind it to the NIC with the device memory TCP feature. That's pretty much what you bind it. And the second control plane is a JIT. So Knot mostly follows.
The same BPF program load workflow like most with the CPU hook path. But the only difference is the since the K node utilizes the GPU for a BPF program execution, so BPF program has to be JIT compiled in advance. So, well, there's the basic path of the BPF load.
Is developer loads an ordinary BPF XTP program and normal BPF pipeline produces the bytecode with the client or and it will run the verifier. So up to this point, this is pretty much familiar with the XCP same load path. And the Know BPF to GPU JIT.
begins after the verification. So Knote custom GPU JIT backend uh takes the verifier instruction and emits the AMD GPU ISA. So developer just writes the XTP and Knot produces the GPU shader. And this GPU shader will later dispatch the GPU with a Knot worker.
Now, let's move on to the data plane and see how the shader processes packets. At this point, the control plan has done its job. With RXQs, packet will flow directly into GPU-backed memory and
program has been compiled. The first step should be done is gathering packet and dispatching PPI pro shader. And when NAPI pause on RX completion, the driver marks a new descriptor into the queue called single producer single consumer.
So what is the uh SPSC? It is a GPU side queue, a shared data structure sitting between the host and the GPU. It carries two things in two directions. The host pushes packet metadata down to the GPU and the GPU hands the result.
of the PPP program, the verdict. The back to host through SEMQ. Each entry is a structure, SPSC BD, page index, offset, and the lengths are the metadata, the host right. They locate the packet in video rank.
Act is what the GPU write back, behavior verdict. By design, GPU side queue behaves as a sliding window over the ring. And here we have stage, weight processed, verdict ready, verdict pending, and free.
Variable to slot is already processed, then it is considered processed. The verdict ready is a packet that actions are already decided, but NIC is still acting on the in-flight region. The vertical pending is the packet that GPU is still processing to decide.
The action. And free slot are new packets that NIC has failed into the RSQ. Between the GPU side queue and the GPU, set the Knode Worker, a connot. It monitors the queues when newly published descriptors appear.
it picks the ready range without moving any cursor, prepares the GPU queue and dispatches the JIT BPIPU shader over that batch. When there is nothing to do, it simply waits for the next publish. The worker is the orchestrator that turns per queue backlog into
GPU work. Now the GPU side. The dispatch shader is XTP program itself, just compiled to MDGPU instruction set. Each work item handles exactly one packet. It reads, it descript computes the packet address in
VDRAM runs the program and writes the verdict into the X field. The computer unit processed the batch in parallel. It directly lands one wavefront at a time, so a single dispatch covers the whole patch. When run complete, the completion signal files and the verdict.
are ready for the CPU to act on. This is available to happen. The XTP program has been treated, compiled to two GPU instruction set and run as a dispatch GDP paper shader, one packet per rain. Each lane returns XTP pass, drop or TX.
And that verdict is applied back to the verdict slot of the GPU side queue. Then the derivator on it, path goes up to the Linux Car networking stack, drop or overt it recycles the page straight back to the RX pool, and the TX retransmits the same video ramp back page through the NICTX queue.
So the GPU decides the queue carries the decision and the driver executes it. Now let's watch the queue as the GPU finishes. It uh in the top ring four verdicts are already decided, drop TX, pass T T X.
And there are rest still work in progress. So the acquired cursor sits right between the verdict-ready region and the verdict panel region. When the Knode worker observes the GPU completion, it advances the acquired cursor. In the bottom
Uh run two more slots have failed from working in progress to pest and TX. So the red region has grown, uh grown, and the pending reason has shrunk. And the cursor has slid to the right. Uh that covers runtime behavior. Now let's go uh
One level deeper into the implementation detail. Where the implementation stands today on the PPF side, array, hash, and port CPU array network, along with the map and XTP helpers, DBMAP, CPU map, ProArray, Ringbox.
Redirect, tail calls, and the PPF2PPF call are not supported yet. That is also why redirect is missing on the NSC side. For drivers, balance supports pass drop and TX, protocol control leaders, drop and TX, and Intel ICE is planned.
GPUs, JFX 9 and 10 work today. 11 is just planned. And CDNA is also just planned. More NIC and GPU support is on the way. Next, how BPI actually maps onto the GPU? The compilation.
Model and excretion model, and how healthers and map are implement implemented on device. Before we go further, a quick refresher on the BPF execution model because everything that follows mirrors these pictures on the GPU. The BPF virtual machine has eleven
64-bit registers, R0 for the return value, R1 for the context, R10 as the frame pointer, plus a stack, maps such as array, hash, and the per CP array, and helper functions reachable from the program. Normally, the kernel runs this with the internet.
Printer or a native jet. So the question for Knot is exactly this. How we migrate each of these pieces, register, stack, maps, halfers, and instruction onto a GPU? The next slide slide answers them one by one. On the GPU side,
Here is execution model using all DNA2 as the example. A chip is a set of worker processors, each with computing unit contains symmetric units. The smallest excretion unit is a vector ALU unit, the lane, and that lane is where one
BPA program instance run, processing one packet at a time. Each lane has up to 256 vector general purpose registers, derivative bit each. So instead of one CPU core running on one program, we get thousands of lanes, each running the
Same XTP program on a different packet. Each SIMD directory runs directly rain in Lockstep, a wavefront. The compute unit schedules the thread groups across its SIMD unit. For K node, this means one wavefront.
32 packets at a time and uh to the batch the workers build webs directly onto this hardware hierarchy. Middle class discrete GPU contains 40 work processors, so 28 computing computing units, each thousand of lanes in total.
That is the parallelogram budget a middle-range consumer card gives us. Up to thousands of packets in flight on the shader at once. This is where the throughput numbers from the measurement slide come from. Register mapping: each 64 BPF register.
Becomes a pair of 32-bit vector general proposal registers. Out of 256 vector general proposal registers per range, from V0 to 21 hold 11 PPF registers, 22 to 69 are runtime scratch, from 70 to 100.
127 cache the packet. And from 128 to last, register hold the whole PPF stack. So the PPF stack lives entirely in registers. Stack read and write never touch memory. This is the
Our next step, control plane, migrated from the confusion driver to the user mode queue interface, move the JET compiler into block implementation, and demonizing the persistent shader data plane. A data plane is that improved occupancy, for example, moving BPF stack into LDS.
Storage supporting of handling NIC's VD on GPU shader and page pool and the NIC lifecycle implement tooling has many things like enable profiling for the GPU shader and widen subtext coverage beyond attach and detach web operation and loops.
All the materials is public. The RFC patch series on the mailing list and the last episode and the related proposals are linked here. If you want to try Knode or review the code, these are the entry points. And other truly welcome feedback and testing.
On other NICs and uh GPU. So, uh any questions? Uh uh multi-server and multiple GPUs
Uh so you're uh referring that uh there could be uh is there any multiple GPU support for this Knot? Right? Uh so you mentioned that there's a performance bottleneck uh f regarding
So would that be uh possible to solve it one for GPUs? Uh I'm not sure, like I didn't get the question. Yeah, I think uh it up to the class of the GPU. Very entry-level GPUs like uh it contains uh uh
12 computing units, and there are Xtrem GPU cards. It contains over 64 computing units. The power is enough that I think there is a bottleneck. And as you mentioned, we can solve the bottleneck with multiple GPUs. I think the software is not ready for.
The multiple GPU, but we already have very early stage software code, but we needed to do much more. But instead of supporting multi-GPU, we are focusing on fixing and finding the resolving the bottleneck in the single GPU.
GPU. I think there is a problem about scheduling. It means GPU has very powerful computing units, but the scheduling is not enough powerful. Only one kernel is working at a time. So the free
The gap between previous GPU kernel and the next GPU kernel, this is the point. We want to avoid the gap, actually zeroing the gap between the kernel, but I think there are many huge gaps. I think this is the problem.
I think. Yeah, thank you.
On other modules? I mean, is that compiler, JIT compiler, is only for K node or other models can use it also? The purpose of the K node is for the networking, but the origin purpose is to use GPU in the
For the other subsystems like crypto, block layers, but our software currently supports only the networking. Thank you.
What is the performance? What is the performance of this uh implementation?
take performance as a packet processing speed but also the one of the important factor is the we have offloaded the CPU packet processing to a GPU so you could also get get an
Let's see. More CPU for you could spend spend the CPU for um more application uh stops. So yeah, so the performance that we are aiming for right now is the reaching for the full line speed. So with the uh 100 gigabits, it's like one hundred forty eight. Uh
Million packet prof process, but we are still halfway there, so we have to still like yeah, improve more. Yeah, I think, yeah, that's still one you're referring to. Thank you.
Uh let's take example uh like GPU already.
Already consumed by some processing is completely consumed and CPU is that time idle. So, how you are going to distribute that packet processing between GPU and CPU? It is not like every time CPU is idle and GPU is like some scenario will be there.
So how do you that software can manage and how that hardware can manage those things like distribution of GPU and CPU offloading in such a way that bottleneck of latency and bandwidth will be proper? Is there any plan or something you are going to do in that one?
So the scheduling stuff for the like if you have um like light a lot of idle CPU or a lot of CPU, uh GPU idle, then you could schedule the stuffs like later man, for instance. So that's uh like uh future plan for us, but definitely uh
Definitely useful for but but for for the now we are like only just in the RFC stage. So like we're trying to only focus on the improving until the line rate. So that's our like primary goal. And the one that you mentioned will be our
Our like future goals for like maybe using scheduling stuff for maybe migrating stuff from the CPU to the GPU. Yeah. Yeah, thank you.
Thank you for coming here. We hope that you have get some insight with GPU and some GPU offloading networking packet processing stuff. And thank you for coming.