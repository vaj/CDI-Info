Memory_technology_optimized_for_at_scale_AI_systems_Bandwidth,_Capacity,_and_Connectivity

Good morning. Thank you very much for being here so early. Hopefully you didn't get stuck in traffic.
Should we talk about memory? I don't remember. Yes, memory is the thing, right? Without memory, you can't do anything. Okay. Please go ahead. Okay. So let's get it started. Today topic is really, we're going to talk about, let's just move to the next slide, actually.
Because that gives the outline. How does the queue this thing work? The moving? Okay, yeah. Yeah. So, we're going to, this is the outline for the talk today. We're going to talk about levels of memory tiers in our AI infrastructure, memory growth.
Both drivers that are requiring scaling of memory across all the tiers. Then software frameworks that are commonly used for AI inference serving. There are plenty of other frameworks also in the space. Then CMEC will talk about a reference cluster.
Candidate cluster for memory scaling. This involves optical links, and it's going to talk about some considerations in putting together an optical link-based cluster and some address and challenges there. So let's start with the base server node. How does the tiering?
Of memory looks in the base server node. This is a very simple view. This is how it was before new interconnect technologies that expand memory came about. And in this one, also, we're not talking about distributed applications. It's really, you have caches, you can call them as tier zero.
then you have you know you know DDR memory attached to the server which is tier one. Then you have a local storage, we call it tier three, and then you have storage, so T T R two and then you have a storage that is connected to one network which is tier three. So you see a tier zero, one, two and three.
So, this was a simpler view earlier. Now, if you look at how it's expanded over time, and final one is really staking the ground for essentially for AI clustering, how one can view this whole hierarchy. But before we get there, let's look at how it's going to be.
Even server side, things have evolved a bit. So now, of course, you have caches, you have CPU attached memory, tier one. But now there is also a concept of this memory expansion. This new interconnect C Excel is used for expanding memory.
In server node. So you can call it actually tier 1 plus, but you know, terminology could be different. This is what we are saying here. And then you also have the notion of having a memory fabric attached to the server node based on C Excel. So that can be considered. It's a longer latency. Essentially, the way to think about
This stack is that as you go down the stack, the characteristics are that your bandwidth is going down, latency is potentially increasing, but the capacity is also increasing. So if you continue with that theme, then essentially this CPU C Excel fabric memory.
T2, we call it T2C Excel, then remote memory because if you have distributed applications, then you want to be able to go across server nodes. There are different paradigms for that, but less quality goes over scale-out typically or over your data center networks in a small domain. So then after.
After that, you have a local storage, and then you have storage over a data center network. So that's the hierarchy in a server node. But now, when we go to AI infrastructure, it gets much more, you know, even bigger stack here. And here, now, actually, this concept of that as you go down, the capacity is increasing.
The latency is longer and the bandwidth is going down, may not hold always. But basically, this is bandwidth typically will go down in this view that we have put together here. So, here now, essentially,
So you have tier zero as before. Then you have uh you know new tier one introduced, which is very high bandwidth memory, HBM memory. So we call it tier one. Then you have HBMs across the cluster that you can access over a scale-up network. Tier one, you know.
Scale up. Then you have CPU memory, tier 2 plus. Also, if you have expansion on C Excel, you could create another stack in this way in the stack view, but we're just calling it Tier 2, Tier 2 plus in that level. Then that C Excel itself, you can also go CPU memory across scale up networks.
Then you have memory over scale-out network, local storage, storage over scale-up network, storage over scale-out network, and storage over data center. So this is a much more elaborate view of hierarchy, and now applications call for very active management of memory across these different tiers.
And several factors are driving the growth. I mean, we know that now, you know, with reasoning models, agentic AI, you know, and multimodal models, essentially memory requirements are going across all the tiers. One important thing that is becoming prevalent is that you need to have a lot more context.
For processing, active context, which is basically work in progress. Then there are other types of multi-turn context. There's historical prompts that you have used, historical KV caches that you want to keep around. There is these knowledge databases, and there is a lot of information.
For additional information databases. Model size are growing. I mean, there are some experimental models up to 1.5 trillion parameters. Then, you know, we all know that for recommendation models, of course, you need a huge amount of memory, multiple terabytes of memory going up to 20 terabytes. And then, you know, you have also scenarios where.
Or in the cloud environment, particularly, you want to have many, many models resident nearby in fast memory, in tier 2 memory, so you can pull the models quickly as the needs change. Other factors driving the growth is also this essentially use of a mixture of experts. Now, so if you look at
The mapping of the different use cases there, different data structures into these tiers. The way to think about, first of all, is that up to here when you talk about tier two memory, the semantics is of a load store. We're basically at the target, what you receive is a load.
Store, initiator side, you could natively issue load store or could be initiated by a DMA. But on target, you're just seeing a load store in that hierarchy. But below that hierarchy, on the target side, you are targeting a DMA, and that's servicing the data for you. So this could be in our DMA type paradigm. All the storage.
belong to that category, but also memory over scale out will belong to that category. There are on the right side the different mapping. These are candidate mappings but essentially you know people are probably familiar with that already. The key point I want to bring out is that one challenging area is that the active KV cache needs to reside in HPM memory.
And it really requires more capacity now, as well as as you go really long contacts, even a million token contacts, you need huge amount of bandwidth to just create the next token. So that's a challenge. Let's move on because in the interest of time, let's move on to the next slide.
So, here now, some software frameworks. So, I'm just focusing here on AI inference frameworks. So, there are a few different ones that are quite more prevalent, more commonly used. Nvidia has introduced what's called Dynamo. This is a framework.
That manages memory tiering allows you to manage data structures in different tiers of memory. You can have your policies and you can decide to move around data into different tiers. This framework expands across all the tiers of this AI infrastructure.
structure hierarchy. So scale up, scale out, and then also data center networks. Then NVIDIA has also introduced this EGM, embedded graphics memory, extended graphics memory, where now it always had earlier unified memory view between CPU and GPU.
But until this point, it required the page migration to happen. When CPU accessed some system memory attached to CPU, then you needed to migrate the page for performance reasons. Now in this paradigm, you're not moving the page, but I think you're relying on the higher bandwidth link between the CPU and the GPU.
This could be extended to also cover, but the latency will be longer. It's not a proven thing whether it will work well, but essentially it could extend to potentially C Excel behind the CPU node. Then VLNM is a popular inference serving framework for Gen AI.
So this manages between HBM and DDR and also across the scale-up network. LM cache services essentially, again, between HBM and DDR, it is supplementing your essentially VLLM for doing the KB cache management.
Movement across these different levels of hierarchy. By the way, on VLM, I want to point out is there is also an extension to this which allows you to move the data to SSD as well. Yeah, this is a swap mode of VLM. Okay, so I'm just going to cover this last slide now.
So, we talked about that in AI infrastructure, there's a big stack, different levels of memory hierarchy with different attributes, right? And to get to efficient infrastructure, you really have to very actively manage your placement of data structures across different levels. The right side is a spider diagram that's Meta has.
Presented in multiple places. I took the liberty to copy it here. But one point I want to point out is that now, for LLM serving capacity-wise, it's really increasing the need for that. But the main point here is that there are different diverse sets of workloads, and the question really becomes: how do we come up?
With different platforms that allow us to have optimal solutions for different workloads. And questions become: in this case, is how can we scale tier 2, tier 3 memory? Not only that, there's a pressing need to be able to scale tier 1 memory, which is the HVM memory, for active caches. And then the second really question is that,
You know, when you scale these things, one answer of course is disaggregation, but the question is there's a disaggregation tax. Compute wants to be near memory, and as you disaggregate, you are adding some cost, latency, and bandwidth reduction potentially. So challenge to all of us is really to look at solutions. How do we minimize the disaggregation?
Tax. Thank you. Sorry, it took longer. Thank you very much, Melan. Now, since we understand the taxonomy and especially this spider diagram is trying to show that different workloads require different solving different bottlenecks, we can go to something else that also looks like a spider diagram. But this is the way.
Way now we can connect memory to XPUs and GPUs. And this diagram is trying to encapsulate a lot of topics worth mentioning. GPUs are connected directly to local memory, HPMs. They're also connected to CPUs.
CPUs. In this model, CPUs serve memory as well, LPDDR or DDR. And nowadays, CXL is there, so CPUs can also connect to CPUs through CXL with different kinds of memory. The memory can be DRAM or memory.
Can be extended capacity using NAND flash or compressed. So, therefore, CPUs can do memory pooling on behalf of a set of GPUs. So, that is the first point that we can make. In the past, we have used pooling across storage.
Domains. So, CPUs know how to do that. So, this is another example of how storage pooling can be used and served by CPUs on behalf of GPUs and XPUs for large language models. We have other presentations that speak about how traditional
Techniques of search and database will feed data into large blocks of fully connected XPUs. So, this diagram then shows also how GPUs scale up using their own native.
interconnect and switches or bridges in this case can bridge topology and protocol from native interconnect that the GPU understands to something that is standard such as CXL that brings in higher capacity memory.
So perhaps we can conclude on this diagram, but it demonstrates traditional models use a lot of memory. They do require scalable within the native processor interconnect.
XMP, parallel processing, these are the things that we know. Emerging techniques for AIML don't really need cache coherence. They don't need all of those features that CXL are offering. But the point that I'm trying to make here is because CXL memory is being built,
And it is available. Even GPUs that don't have a direct connect to CXL today through their own CPUs or through GPUs can couple to traditionally provided commercially available CXL memory and use it the way they.
Wish. So, therefore, we will need hybrid concepts. The hybrid concepts can come from CPUs coupling to both worlds or new bridges or switches that become available. So, once we do all of that, you guys are familiar with the pyramid.
Probably are tired of the shape of that pyramid. So we're going to stretch that pyramid and increase capacity at different layers. Everybody wants to have high bandwidth and high capacity. The reason that we have the pyramid is it is hard. It is hard to make both. Now, in this model, using
New memory techniques, we can increase the capacity on HBM. We can increase capacity on bulk memory around CPUs. And now we can increase capacity in bulk memory that is connected to the fabric using CXL. So as we try to do that,
We see that we have challenges in connecting these elements together. We are used to DDR buses, parallel buses, HPM buses, directly connected to the processing element. But we're also getting features such as CXL connected that are.
Serially connected. Because of that, then we can interconnect more things. But in trying to do that, it is also interesting to note that high bandwidth by itself is not going to save us. We need connectivity, we need independent streams of data to go from one
GPU to another GPU, from one CPU to multiple GPUs. And to do that, the techniques such as what HBM bus does or UCIE does, you have a very wide bus, but it is subdivided into sub-channels. And those channels are independent entities that we can route them in different places.
Then we see that as we increase the number of elements in a topology, the higher the number of port counts is, the fewer switches you might need. So today we have a couple of other examples and presentations on this.
Concept that by increasing the number of ports per device, we reduce the number of switch layers, reduce the time required and power required to move data. And then this is a diagram to show that. On the left-hand side,
You see 16 GPUs are in a node, and these GPUs are fully connected through nine switches. And each one of these switches is connected to external switches. So you can see that if you have 16 GPUs in one node, and you interconnect 16 of those,
you get to 256 GPUs, basically fully connected using two hops through the switches. So this is a cluster of 256 GPUs using suitably selected switch of 64 ports.
If we increase the number of ports, we can increase the number of GPUs in a cluster. So, other topics that we can talk about, don't have much time to cover today, are just copper versus photonics. We have seen how to interconnect devices using copper cables.
Cables, but the challenge of bend radius and distance comes into play. Therefore, we believe the next wave is going to be solved by photonic interconnect. In that model, devices or super chips can have photonic engines on them, and instead of using copper,
To interconnect devices, these protocol engines on photonics can help us. To do that, there are a number of companies that have come up with solutions for that. And when you put them all together, these are some of the slides I borrowed from Celestial AI.
Enough to offer it, we can increase the number of things that are connected to each other and therefore have a full rack of fully interconnected GPUs and memory. So I can close on that since we are short on time and
Basically, invite all of you guys to participate in a number of activities we have with OCP. Composable memory system talks about large memory blocks, fabric orchestration, memory fabric orchestration. We have short reach optics that touches on what it takes to actually interact.
Devices using photonics, but different from the long haul that we are used to in large data centers. These are for five-meter, ten-meter distances. And then, of course, hardware and software co-design within OCP is a place to join the effort.
So I close on that. I'm not sure if we have time for questions. There's a question. We need a mic over there. Yeah, you touched on GPU memory expression.
Expansion via CPU, CPU proxying in some way to expanded memory. Could you please comment on the current state of the art as to where NVIDIA and AMD world is proceeding in terms of expanding their GDDR or HBMs? Potentially, could you also please comment on for
For example, NVIDIA's announcement of NVLink Fusion, whether that could be used for expansion or not. I don't think John will let me answer all those, but the answer is yes. State of the art is growing. Specifically, NVIDIA has announced a solution for.
GPU extended memory, which software can use to seamlessly treat memory that is connected to CPUs as its own memory. So that is basically a number of software organizations. Google has talked about memory banks, Fusion.
Uh fusion uh okay. NV link fusion is a totally different topic. We'll we'll cover that one later. If there's any other questions, come up and uh ask them in person. Yeah, I have a quick question. Um where do you see the distance limitation on um uh T1SU?
The tier one scale-up, again, by this definition, scale-up is the interconnect that the GPU is natively using. So if the native interconnect for a particular GPU is, for example, Ethernet-like,
such as UA link or NV link, then you expect that you can stretch that one using copper to about two meters and photonics about short short ridge optics maybe five meters, ten meters. Maybe that is a very good stopping point for a cluster with NRS.
From a latency standpoint, like in terms of how they want to keep the latencies down for SU versus SO? SO, they scale out because you go through a protocol conversion. Software through MPI, they are telling me that we need to drop. But we can't have that conversation.
I mean this is a scale up is not that critical. Latency is important, but tens of nanosecond is is okay to add, right, basically. Thank you. Thank you.
