Kernel_TEE_Subsystem_Evolution_-_Sumit_Garg,_Qualcomm_Inc.

Thanks everybody for joining this session. And I I can see it's late afternoon, so thanks for joining this session in large numbers. So welcome to this session. Uh here we are going to mostly focus on the kernel side of things or the Linux kernel and especially the trusted execution environments.
How we are bringing support for trusted execution environments in the Linux T ecosystem. So, here we are going to talk about the evolution, what it took. So, the driver got introduced in like 2017 timeframe in 4.12 kernel.
We have got like earlier it was just like a single trusted OS that was Opti that was supported. Now we have at least three of them supported in mainline and the fourth one is just being reviewed upstream. So yeah, the support is growing and we want to make it like
Grow and keep it healthy. And that's what we are going to see today. So, first of all, about me. So, I'm Samit Garg, working as part of the Foundries team in the, that is an independent subsidiary within the Qualcomm. And yeah, my
I have been like, throughout the boot stack, I have worked on firmwares, Linux kernel, bootloaders, and whatnot. So wherever the security comes into picture on the BSP stack, I usually get involved. So I have done bits and pieces into the Linux kernel as a reviewer for the T subsystem.
as well as um on the TFA side and opti side and yeah, on the bootloaders, U-boot, EDK2 and yeah, uh everywhere uh so possibly. And uh mostly uh background for me has been security and uh yeah. Apart from that on the open embedded side, yeah.
I've been involved in the MetaArm toolchain effort as well. So yeah, that's all about me. Let's let's get started about what is a T. So T is a trusted execution environment and what it really means is like it allows uh the applications that are running in the rich environment
Rich environment, we usually call the Linux environment, where we have like hundreds of applications running alongside. And what the trusted execution environment offers is the isolation. So basically, you are able to isolate the security-critical code out of.
Of your main application code. Say, for example, some code within your application is dealing with passwords or keys and credentials, and you want to protect that code from a huge number of attack vectors that are there in the Linux in the Linux.
Linux user space, basically all the applications running alongside your security critical application that has to be protected from like all the various attack vectors and that's why uh this trusted execution environments come into picture and they they allow like hardware and forced isolation of your
security critical code to run. And uh it is supported by uh various hardware uh based mechanism like on ARM. Uh we have this ARM Trazoon and it is like it has started uh in like two thousands during the mobile era and uh where people like
in 2004 or sometime at that time frame, it started. So ARM introduced this trust zone support in the ARM v7 architecture and it was in some support in ARM v6 as well. But yeah, that was there and applications were developed using this.
Especially in the mobile space, you might be using payments applications or DRM applications. You would be playing some DRM contents from Netflix or YouTube or Amazon Play Video, etc. So all of them are using.
Mostly the trusted execution environments that are allowing to keep the keys and the content that is being played on your phone secure. Basically, the users are not being able to access the media pipeline in secure format.
It's you are playing a movie on your phone, and you can't see it in the plain format from Linux user space, but it's like it's displayed to you, but you actually can't copy it directly. So, those kind of stuff that are enabled by this trusted execution environments. And the underlying technology.
As I mentioned, Arm Trust Zone is there. There is also this coprocessors that are supported on AMD platforms. And nowadays, with RISC-V, they are coming up with this trusted domains that they support. So, yeah, these are all the technologies that are supported by hardware.
From the kernel perspective, we abstract these technologies to the user space so that your applications can work with all those hardware-backed technologies in a more abstracted way. So, yeah, the other thing about from the application point of view, what you
Need is like uh portability. Say, for example, you have written your application code for say for example, with ARM Trust Zone, it should work with something like RISC-V trusted domain. So, global platform is something that is providing all the specifications so that the application writer.
Can write their code so that it should be compliant when it's running on an ARM platform or a RISC-VI platform or an AMD platform so that that is portable. And it is providing all these APIs here. This is the specification link I have put.
Here, so that all the trusted applications or the client application can be written in a portable format so that they could run across trusted OSs or the trusted execution environments. Now, let's look into the overall architecture of a trusted execution environment. This is taken.
From the global platform specifications itself. What it shows is like you have the Linux world. Oh, sorry. You have the Linux world here. This is the normal Linux OS, what we call it like regular OS or a rich execution environment. We have the normal world.
Applications, Linux applications running here, and we have an application, like client application, what we call it, where what we need is like we have some security critical code that has to be isolated from this client application as a trusted application in this trusted execution.
Environment. So think of it like this code used to be as part of this client application. But the hardware isolation can allow you to separate this code from your main client application as a trusted application. And there is a shared memory view among themselves.
from client application and trusted application and this is the application which is handling all your security critical operations. Say for example managing your keys or performing some crypto operations on top of it. And that's that's how we can this is the boundary that the hardware enforces.
So we have this hardware enforced boundary. It can be ARM trust zone or RISC-V trusted domain or AMD coprocessor or whatnot. But the overall idea from the system architecture point of view is that we have a real isolation in between, that like the other normal rich.
Applications can't access the memory contents of this trusted application directly. So that's the overall idea. The underlying framework is just about the communication path, how it happens, and how the client application gets to talk to the trusted application. And these are the standardized APIs.
Global platform on the client side and on the T side on the trusted execution environment for a trusted application developer, there are this global platform T internal APIs. And REST, this is implementation-specific, all this rich execution environment complex.
communication agent and that's where this kernel T subsystem is coming into picture on the Linux side. So we are abstracting the communication here towards the whatever like trusted OS is supported, say for example on different silicon vendors on
Like Opti or Qt or like Trustonic or anything else. So, yeah, these are all different trusted OS implementations. And yeah, this message passing is based on the hardware mechanism. It can be a mailbox, it can be SMC.
Calls what is offered on ARM Trust zone. So, this is all implementation specific. And coming on to the real hardware, the hardware is also isolated. Say, for example, we have the normal peripherals, PCI or USB, UFS, and anything. So, those are the normal peripherals, and there are
These trusted peripherals as well. Say, for example, your fingerprint reader on your phone or your display that can take inputs from the user, like passwords or things like that. Or you can have like the crypto accelerator can be a shared peripheral because both the worlds need to perform crypto.
operations, then yes, that can be a shared peripheral. That's just an example. There can be other peripherals that can be shared among both the worlds. And yes, so that's so trusted OS is mostly about, yeah, we have the same user space kernel boundaries in the trusted OS.
And we have the device drivers along with it. So, Opti is an open source implementation for this trusted OS. Now, coming on to actual realization, how this T is being realized. So, as I mentioned, there could be different realizations.
Here, what it this picture is showing is like we have this external security SOC. When we talk about discrete TPM, we can think of that like this kind of implementation where it is an external security SOC that is that is separate from the main SOC system on chip.
And there is communication established via SPY or via I to C uh to this external security SOC. So TPM is very like a peculiar example here because it has a defined stack that is already built into it, but we users can't run their own application code.
On the TPM, but yeah, any other external security SOC which offers such kind of environment can be treated as a T. Now, the second one, hardware realization, is actually the most commonly deployed. This is like the shape.
Shared components. Here we have the actual SOC that is being partitioned into like trusted peripherals and the normal peripherals. Here the blue fields indicate these are assigned to the trusted execution.
Environment, these peripherals are assigned to the trusted execution environment, and the other ones are to the normal, like the Linux operating system. And similar with crypto accelerators, one-time programmable fields as well on the SOC, and the on-chip RAMs, external memories like DRAM, can be like, they could.
Be hardware IPs, trust zone IPs on your SOC, or any other hardware enforced boundary or firewalls that are existing on the SOC. So that allows this realization to be there. So ARM trust zone is a realization of this T, and similarly, risk.
5 is also a realization of this kind of T environment. Now, coming on to the third realization, that is the on-chip. So, there could be an on-chip security subsystem as well. That is having its own peripherals that are separated from the chip.
From the main peripheral that are accessible to the main processing cores. So, this on-chip security subsystem is like: say, for example, we have a small core sitting alongside your main processing core, and it has its own subsystem. Say, for example, we have the AMD core, that is the x86 core, and we have.
an ARM core sitting alongside it that is a core processor and that is having uh this security subsystem being implemented. So that's what AMDT um implementation is all about. They have the X86 AMD processor and the ARM processor is there that is providing all the
T operations for the application. Now coming on to one of the open source implementations. So traditionally it has been like most of the trusted OS implementations have been uh like closed source and they haven't been open. So OPT is one of the implementation that has
That is from the beginning, it has been open source. And it was open source in like 2014, so from Lenaro. And Lenaro is the one who is maintaining bits, maintaining the Opti support. And the main design goals for Opti is, yeah.
Isolation is the main one, and the second one is the small footprint. So, small footprint is very essential when we talk about trusted execution environments. Because why we call the Linux as a rich operating system is because it is providing all the drivers, they are everything in the system.
There, the trusted computing base is very much enlarged. So, we need this trusted execution environment to be as minimal as possible so that it is auditable, it is verifiable, and people could trust it. If we rather say, like, we keep on adding drivers in the trusted execution.
Execution environment, then it would bloat it up, and there is no meaning about being called a trusted execution environment. So keeping it small is the main thing, and that's why we have these services that are provided from the Linux operating system. Say, for example, file systems.
services or the other I/O related services are rather offloaded to the Linux operating system and we keep only the security critical operations within the trusted execution environment and that's what the design goal has been. And third is the portability, as I talked about the global platform specification.
Opti is compliant with the global platform specifications, and you should be able to write your application code with Opti that is then portable to other trusted OSs, which are again global platform compliant as well. And for you to get
Started with Opti and use it. You can just go and use Camus here. You don't need a real hardware to get started. You can experiment with Camu. Camu is an open source project again that is very useful to experiment with.
Opti support is available. There is ARM Trust Zone emulation that is there with Camo available. So you can go ahead and run your CAMU with Opti support and experiment there, write your trusted application code. And once you, so the trusted application code is very portable. So once you write for CAMU, it is like, it should.
work for a real platform. Now coming on to the overall opti architecture. So here is where the kernel subsystem comes into picture. So it is providing the underneath like framework to talk to the trusted OS itself.
So as you could see here, we have the application code here, and this is the T client API, which is compliant to the global platform API specification. And then there is on the kernel side, we have this generic T API octals, and that is provided by the T subsystem.
And underneath T subsystem, you can have implementation specific drivers, like Opti driver is there, AMDT driver is there, and similarly other implementation drivers can be there. So here we have this Opti driver. It is using its own like Opti message SMC call interface.
To talk to the trusted OS. So, this is like an ABI because these are totally different binaries that have to talk to each other. So, we need to maintain an ABI that is stable and backwards compatible. And here on the trusted OS side, we have
There is the internal API that is again global platform API compliant and trusted applications. Here we have this concept of pseudo-trusted applications. So these you can think of it like how you treat a kernel module on the Linux kernel side. We have this pseudo-
You do trusted applications that could be platform-specific things or services that have to be offered from the trusted OS side to the Linux side. So, but yeah, these applications have to be compiled into the opti source code rather than truly loadable modules. It is like built into the Linux kernel, opti OS side.
And for reference, I have uh put a link here if you are interested and go throughout that uh opti implementation. Now, uh so with that opti implementation, uh the kernel T subsystem landed in the Linux kernel and it was 4.12 kernel release when the kernel
T subsystem was there. So it provided this registration and unregistration of T drivers, shared memory support. The shared memory support is something that is critical and that can be shared among different trusted OSs. So say, for example, we have like a common API call.
From the user space to share memory with the trusted application. So, one client application should be able to share memory with the trusted application. So, that kind of shared memory support is there. So, in the initial time, it was only ARM targets for Opti, and we added reserved memory support, and there is this.
T supplicant support. T supplicant is something that I talked to you about. We don't want all of the drivers to be implemented in the trusted execution environment. We rather piggyback on top of the rich OS to provide, say, for example, file system services, networking services, and
and similarly any other services. So we offload those non-security critical services to the Linux world. And that's why uh we need this T supplicant or or any other like services that can be offered from user space um uh support in uh in the user space.
Space. So yeah, that's about Opti. And now coming on to evolution with the AMD. So first target was Opti, then the AMDT came into picture and that was added in upstream. So similarly, as I talked about Opti driver, then we got this AMDT driver.
That is hooked underneath the T subsystem, and this uses this mailbox framework to talk to the ARM Q processor that is sitting alongside the x86 processor. So, this is the third realization that we talked earlier about. And there is this AMD trusted OS that is running on an ARM.
Platform and the Linux environment is on the x86 CPU. And yeah, similarly, the trusted applications here are again written with the T internal APIs and the client API. So overall, the framework remains the same, but yeah, the driver and implementation can change.
underneath. And similarly, the other RISC-V architecture implementation, they are still in progress and people are adding support for that. But yeah, it hasn't landed anything upstream. So for AMDT, you can go and look into this reference and to know more details about it.
And then comes in this T bus or the internal clients. Uh first of all, earlier we had only the user space clients for the trusted execution environment, but there are also uh in kernel use cases for the trusted execution environment. Say for example, you need
You have some firmware TPM support that is running on the trusted execution environment side, and you want services from it within the kernel. Or the other use case can be hardware random number generator, which is not like shared, which is not accessible to the
Linux kernel at all. It is only accessible from the trusted execution environment, and you need to provide entropy to the Linux world as well. So those kind of use cases are there. And similarly, there are many others as well. There can be platform-specific use cases within the kernel.
And that's where we realized that we need an in-kernel client as well for the trusted execution environments. And that's where the T-bus came into picture. And here on the T-bus, this is like a normal like Linux kernel bus and where you where we can
It allows like enumeration support that is very much specific to the trusted OS because every trusted OS has their own services, how they can register on the T-bus. And from the driver's point of view, it's like they register on this T-bus for any device.
To be present, and it's the similar Linux kernel driver framework that we use for other kernel drivers in other subsystems. So, in the kernel T subsystem, we are providing these, enumerating these services on top of T bus. And there are like we have this match API.
The match API is important here. That allows you to write your kernel drivers. Say for example, uh I want to communicate with Opti to get a service, then yes, uh there would be a match corresponding to opti. Um if there is like for Qt people have to write or for AMDT people have to write a driver, then yes, uh
the simil the match API would help you to write your driver accordingly. And uh then there is UEvent that is just like uh allows you to auto load your drivers based on like if a service is provided by the trusted execution environment for the kernel, then the corresponding driver would be probed automatically.
So, the T bus enumeration, as I talked about, so this enumeration is very much specific to the implementation. Currently, we have the support for Opti within the kernel, and that allows you to enumerate all the services that are being offered by Opti and various drivers.
Can talk to Opti using those services. And all these services are enumerated as devices named as Opti-TA and UUID. UUID is the identification of that service, basically, unique identification of that service. And that's how Opti registers the devices on.
On the T-bus, and using the match API, the drivers can talk to opti devices. So this is the hardware RNG use case I was talking about. So there are like silicon vendors and implementation that allow the hardware RNG.
To be used or accessible to the trusted execution environments, and we need to provide a service to the kernel to allow it to get the entropy from the hardware random number generator. And this is the interface how we did. So, this hardware random number generator driver is sitting on top of.
The T-bus and the service is enumerated by the opti driver, and that gets enumerated on the T-bus, and the hardware RNG driver can then invoke APIs to fetch the entropy from that as mediated by trusted OS for the hardware RNG, and then that could be that entropy can be.
Fed into the entropy pools of the kernel and um either in the user space as well as dev hardware RNG note. The next use case is about trusted keys. So yeah, you might have heard about the TPM sealed keys or TPM based
Trusted keys in the kernel, and that was a framework that was added earlier, using TPM as the source of trust. But on like embedded platforms, there are like strict bomb course constraints. They might not be like every time TPM is available. So people would like to
use alternate mechanism uh for the trusted key support. The essential thing about trusted keys is like the key content is not accessible to user space in plain format. As you could see here, here the key is in a blob format only or a wrapped key and user space can't access it in the plain format.
But the kernel can access it in the plain format using the trusted keys driver. So here we extended support in the Linux kernel to support different backends like TPM or a T. Then with the T, what happens is like this
Driver would reach out to the trusted OS. It's similar, like it's sitting on top of the T bus and it would be communicating to the Opti. And here, Opti would be the service would be implemented as a T device, as in trusted application. So here it is added.
Using the hardware unique key. This hardware unique key is only accessible to the trusted execution environment and not to the Linux environment. So, this key is the master key that is being used for wrapping whatever key gets generated here. So, say for example, kernel here generates a new key and it has to be wrapped.
Then this hardware unique key is used from Opti as a service so that whatever key is being generated here, it's not accessible to the user space in plain format. The user space only stores it as an encrypted blob. So, because you need a persistent storage for those keys.
Keys. So during the lifetime, user space is not able to access it in the plain format. Only the kernel is able to access for all the like how you program your crypto operations or the hardware crypto accelerator using that key. So yeah, that is another use case for
This T-bus. And the third one is the firmware TPM. So, as I said, discrete TPM is not available everywhere, and firmware TPM or virtual TPMs are being increasingly used, especially on phones. You would see that
There is a TPM also available there, but as a firmware TPM implementation, and even on compute platforms, people are go started to implement these use cases as a firmware TPM rather than as a real TPM. So, yeah, similarly, Opti has an implementation of TPP.
TPM that is running as a trusted application. So here if you see this, so Microsoft was the one that uh led this effort initially. They created this trusted application for the Opti environments and uh after that uh the Opti
Maintainers fork it as Opti F TPM, and now it is currently being maintained and being supported upstream. Now, yeah, this is the driver that is there in the kernel that is going to talk to the firmware TPM implementation.
And this is again sitting on top of the T bus within the kernel. So there are also other platform-specific use cases. The SCMI transport for OPT, that is like for system control and power management.
uh that is an ARM specification. So you can have uh an SCMI server that is running as an opti trusted application. And from there you could manage like uh like power management for your peripherals. Say for example your GPU uh basically voltage and frequency.
Scaling for your different peripherals can be done using this framework. And similarly, there is EFI runtime variable support using RPMB that is being supported upstream. That is again, because the secure storage being offered via RPMB for EFI variable, there is again a driver that is sitting
In the kernel, that is using the services that are provided by Opti. And other Ts can implement the similar services as well. Similarly, the real-time clock. So, this is again, this real-time clock is not accessible to the Linux world, but only to the T environment. And yeah, there are.
Platform-specific service. Similarly, there's this remote proc T driver where people would like to have, yeah, there could be multiple firmware. So today's SOCs are very complicated and people want to load. So there could be coprocessors running alongside for different use cases. So you need to load.
load their firmwares and there are use cases where you need to validate those firmwares before loading them so the T environments can offer services like validation of those firmwares or secure firmware loading. So yeah those kind of use cases are also supported by this T subsystem.
Yeah, this was another T that was added over FFA. Yes, so we have this trusted services running with FFA and that is also exposed to the user space. This is the library that is being used, lib trusted services.
The subsystem remains the same. It's just like the FFA T Trusted Services T driver and the FFA driver is there. So you could look into details here. Although I'm not going to talk into details about it. Similarly, the QT driver that is being recently pushed upstream, it is still under review.
And uh here uh it's again a different trusted OS that is running in the ARM trust zone world. And we have this QT SMC uh Q s Qt message SMC invoke uh interface and we have this Qt driver underneath. And here we have a different library that is libcolcom T that is sitting on
And the client applications are using this. So, yes, the libraries would be different, but yeah, the subsystem is offering all the services that are needed to realize the implementation here. Now, coming to the last one, I have a lot of different things.
have here for the use case is the protected DMA buffers. And uh this is one that is being there. So I I talked about uh playing a secure media pipeline on your mobile phones on our your laptops. Currently you can't do that with the upstream Linux on your Linux laptop. So
Uh it has been a decade of work that people are trying to push uh use cases upstream, but the bottlenecks is like there isn't like end-to-end open source use case uh for it, and um especially on the DRM side, uh that is all like mostly closed source implementations.
But what we want to say is like people should open up those user space environments so that with upstream Linux we are able to realize those use cases as well. So yeah, the DMA heaps that were being proposed earlier that were carve-out heaps or
What people call it. So the one bottleneck was end-to-end open source use case, and the other one was the metadata problem. So every silicon vendor out there, so this secure DMA buffs are like how the firewall restrictions are implemented on like
That differs from one silicon vendor to another. Say, for example, NXP has a different way, MediaTech, Qualcomm, or any other vendor would be having their own firewall solutions, how they implement firewalls on their SOCs.
Leads to this metadata problem because with this DMA heaps that were proposed earlier in upstream, people were, it was difficult to like pack in the metadata, say, for example, for NXP silicon or for MediaTech or for Qualcomm Silicon or anybody else. So, yeah, those files.
firewalls or how the memory is being secured. That is all like vendor specific things. And we don't want to expose that information to the user space because user space doesn't care about a vendor specific implementation, how the actual memory, how actually memory is secured.
So user space only cares about the abstracted interfaces. So yeah, that was the problem. And right now we came up with this protected DMA buffs in the TE subsystem. What we did here was like we abstracted out the vendor.
Specific metadata to the back-end team implementation. Say, for example, Opti has its own way of abstracting out the implementation for different silicon vendors. And similarly, for QT, it can have its own way of abstracting that out. But from user space,
Side, we see a common heap being exposed, whether with Opti or QT, and the secure media pipeline or other use cases can be built on top of it. So, other use cases can be like if, say, for example, you need to perform some secure keys or crypto operations that.
Can also be done using the secure DMA buffers. Like, say, for example, you have a wrapped key with a trusted execution environment, and you just need to unwrap it in a secure memory that is only accessible for your crypto accelerator and not to any other kernel level.
Linux kernel or the user space. So that kind of use cases can be realized as well. So yeah, this is work that is already under review in the Linux kernel opti mailing list. So this is the overall video pipeline, how it would look like.
Like using GStreamer as a user space application can be a reference for realizing these secure media pipeline use cases. So, here what we are proposing is like we have the single protected secure video DMA.
Heap that is exposed to the user space. And say, for example, your encrypted content comes in this system heaps here. And this content is in encrypted form and it comes into plain format in this secure video buffers that are not.
Exposed either to the kernel or either to the user space. This content is only accessible to the media pipeline, only the crypto accelerator, hardware decoder, and the display engine. So, how the key gets provisioned into the crypto accelerator, that's via the trusted OS communication. So, the trusted
OS would provision the key into the crypto accelerator. And the crypto accelerator is the one that is able to access this content as well as these buffers. So crypto accelerator would put the plain content here after decryption. And afterwards, these buffers, this buffer can be shared with the decoder.
And the display pipeline. So, why we call it secure? Because the kernel is not able to access it, and the user space is not able to access the content in the plain format. So, that's mostly the DRM use case is all about. So, yeah, that's it mostly for me.
And yeah, the final thing. So, future work is always like keeping the T subsystem healthy. And we want end-to-end use cases for this protected DMA buffers and more T implementation drivers as well. We would like that. And yeah, because there have been a lot of
Of trusted OS vendors out there. We want them to push their drivers upstream. And let's work collaboratively. Yeah. That's it. Yeah, if you have any questions, I think I have overrun mostly. Yeah.
If you can go back to the previous slide. This one. So the crypto accelerator code, is it the secure memory under the T context, or it's going to be part of your non-secure memory with the regular code context? So there are two parts to the crypto accelerator. As I mentioned, it's a it's
It has to be a shared peripheral. So both like the trust zone should be able to access it as well as the Linux world. From Linux point of view, the data path has to be there. From T world, there has to be a control path. Say, for example, provisioning the keys is a
Control path and that's not available to the a Linux world at all. Yeah. Uh uh s okay. The uh but the software part, right? The driver which is provisioning, that's going to be in the T only, right? Yeah. It's not going to be in the yes, yes, it has to be the software driver has to be there. But it is only like it can't provision the keys.
But it can like say where is your DMA address from where you can like do the decryption and all. So basically the DMA address for this secure video buffer as well as the encrypted buffer has to be passed to this crypto accelerator via this driver.
So running this secure media pipeline, right? I mean in trusted execution environment versus in rich execution environment in a protected VR.
Are there any advantage and versus disadvantage you know implementing secure media pipeline in a rich execution environment which is protected and versus running in uh T? So as I talked about earlier, the T is something we need to keep it minimal.
So getting things out of T is always good practice. Only implementing the control paths, which is like key management, things like that. That's what the T should be all about. It shouldn't be like dealing with content or things like that. Whatever we can move out,
To the Linux world and keeping the TCB for a trusted execution environment a minimal, that would be always good to have. So, do you mean the performance and all that you get in a protected VM environment running a secure media pipeline? Probably that you won't get.
Performance is also another factor because there is overhead when you switch among this Linux world and the trusted OS. So there would be a performance overhead of these uh switching worlds among for CPU to switch from non-secure mode to the secure mode. So yeah
Having all the stuff that is not security critical outside of the T is better. Yeah. Okay. Thank you. So if we have several
Different implementations of TE, so GPD and the QTE, and both of them support the secure playback use case. Will the interface from the user space to the TE driver the same for those two implementations, or will they be slightly different? And where do
We wrap that for the media players, etc. Sure, that's a valid question. I think that's where this heap is coming into picture. And that's why earlier what people tried was like: yeah, make these heaps as an independent heap outside of the e-subsystem.
As normal DMA buff feeps and making it secure with different interfaces to QT or Opti or anything like that, that was hard to do. But what happened is we moved the allocation and the security within the T subsystem, abstracted that out from
Here and all the DME buff operations where like actual access control for the crypto accelerator or hardware decoder and anything. So that is all abstracted as DME buff operations from the TE subsystem. So from user space point of view, it's like just passing on this DMA buff.
to the crypto engine and the crypto engine when it would attach to that DMA buff at that point the corresponding access permissions can be set. There are also like implementations where the dynamic access control isn't needed. All the pipeline is already configured.
So yeah, that doesn't have need to be open to the user space. So from user space point of view, it's like a buffer that has to be passed to the crypto device or the video decoder or anything like that. Nothing has to be open more than that. Yeah, the OS should abstract all that information.
information out for the user space. So that's what our goal is. So here what we are doing is like the trusted OS driver would uh be implementing all those DMA buff operations. Say for Qt it would be implementing its own, opti is implementing its own. So yeah, that's all abstracted from the T implementation point.
Of you. Yeah. So you talk about the EFI firmware-specific drivers. Yeah. Can you throw some more lights on that EFI runtime variables and how it's more related to a T, if possible? Yes. So
Especially on embedded platforms or IoT platforms or the edge devices, we have the limitation. Like, we don't have multiple storage devices available on the platform. On a typical server platform, we would have a separate storage media.
Medium, which can be used for the EFI variables that is owned by the firmware. But typically, on an embedded platform, we have this shared storage media problem where, say, for example, we only have the EMMC flash available, or say, for example, we only have the UFS flash available.
On the platform, and the problem comes as like the firmware and the OS has to share that storage medium. So EFI variables have to be stored on the same storage medium as the kernel is having its own root file system. So the driver would be common.
So once the firmware hands over to the Linux kernel, it would be just the kernel driver which would be owning the UFS or EMMC or things like that. So this implementation allows the kernel the EFI variable storage to happen in the kernel itself. So when the
Firmware hands over to the kernel. So, the kernel can override those services to provide the storage from UFS. Say, for example, we currently have the RPMB storage. So, that is secure storage for authenticated variables use cases, and that is supported via the T.
Basically, RPMB is something where the keys are owned by the trusted execution environment, and you go to the T to access the RPMB on your UFS device or your EMMC device. So that's how that is implemented. So it follows the secure.
Boot process altogether, right? Yeah, yeah. Basically, secure storage is being offered by RPMB, and that's going via T. Okay. Thank you.
