Towards_Real-Time_NVMe_Monitoring_nvme-top_-_Daniel_Wagner,_Nilay_Shroff

Thank you very much, everyone, for joining today's session. So good morning, good afternoon for the people, or good evening who have joined remotely. Okay, so today's topic is about towards real time LMM monitoring using LMM etop. And I'm Nilai. I work with IBM Linux Technology Center. So Daniel is also supposed to be the call speaker today.
But for some reason, he's not able to travel. And so I'm alone here to present this. Okay. Okay. So, what is the motivation? The first thing we'll talk about: the motivation for this tutorial.
So, as we know, that for NVMe today, we have a command called NVMe CLI. The NVMe CLI do provide us a very good output, but this is all a static snapshot because you build in-memory top.
Topology for your NVMe and then present it to the user, right? So, which is all static, there is nothing real. So, what we need is some tool which can give you a monitoring in real time. Today, if you want to debug some issue with the NVMe, and particularly with
With NVMe fabrics, most of the time, what we see is that the user or the developer has to run the NVMe command again and again, and which is very slow and kind of reactive. So we see that there is a gap here which we need to fill by giving the user the opportunity to, you know, see or monitor in.
Real time, what is going behind the scene? So, today we have the command like Iostrat, for example, yeah, IO top as well, which would give you something in real time, but that is not NVMe topology or NVMe multipath aware, right? So, if you have so many disconnected to your system, it would give you the output in the real time with some information, but that is not NVMe multi-path.
Path or NMAV topology or that. So there is a gap there, and we are trying to fill that gap with this tool. And we call this tool NUMAV top. So that will help you give you the continuous visibility, not the snapshot, in the real time. So I'm going to show you some dashboard what it is going to present in the subsequent slide.
Okay, so first, just I will give you a brief introduction about the design. So, the NMI top UI design inspired from the top Linux command, which we all use day-to-day. So, NMI Top UI has been divided into two levels. So, the level
One is a first dashboard which you will see when you run the NVMe top command, which will give you the subsystem summary. Okay, so for example, you may have connected so many disk to your host on the level one screen when you run the top command, it will show you the summary of the subsystem and you.
Using this summary, user would be able to see what are different disks probably connected to my host. And then there is an interactive menu using which you would be able to select the subsystem which you want to focus on. It is not possible to give on one dashboard due to the limited space on the screen.
I would be able to show you all the information at once, right? Because it will be really exhaustive. I won't be able to fit everything on one screen. And so, the first level screen will give you the subsystem summary using the arrow key, the interaction manual. You should be able to, you know, select the subsystem which you want to zoom on. And then in the level two,
Where you are going to drill down and see lots of detail about your subsystem, which would include, for example, your path state if it is multi-path, path health, controller summary, as well as the namespace head stats. All those things would come on the second screen. So I will show you everything in the subsequent slide.
And yeah, please interrupt me in between if you have any questions because I want to make this session interactive. So you have any suggestion, feedback, or any query or question, please stop me. Okay. Okay. Yeah. So this is the level one screen which I'm showing you here. Okay.
Again, user would be able to also select the delay between your or it is a refresh interval. Okay. So at the first level screen, this is what you would see. This is the snapshot I have attached here. So as you could see here, there are on this system, when I took the snapshot.
It has three subsystems: subsystems 0, 1, and 7. The second column shows you the number of namespaces for each subsystem, number of paths, then the number of controllers in the subsystem. What is the current I/O policy you have configured? And then, if you are running any I/OS, it will also show you in real time what is the current IOPS.
For read and write, what is the latency observed for that particular subsystem? So, when we are looking at this view at the subsystem level, it is aggregated, right? So, if you have multiple namespacing the subsystem, the value you will see would be the aggregated values for the latency or IOPS. And similarly, what is the bandwidth and utilization at this point of time? So, this is the first screen you will see.
And you see, down there is a user interaction here, the up, down, narrow key, using which you should be able to navigate the subsystem. Choose the subsystem which you want to focus on in the next level. If you want to quit, you should be able to press Q to quit it. So, this is just one summary per subsystem level you will see at the level one.
Okay, okay, in the level two screen, there will be a lot of information. Okay, this is not the only information which will be printed or shown to the user on the dashboard at the level two, but this is the header which you will see on the top. Okay, so at the top,
Of the level two dashboard, you will see at the top what is my current refresh interval, the name of the subsystem, NQN, host NQN, the IO policy, which we have configured for the subsystem. This is the header, okay. This is not the complete dashboard. So, this is the first part of the header which you will see.
Yeah, in the next thing, just after the header, we will see this is the namespace head state or the namespace head table. So, how many heads do you have? If it is a multipath, right, then if anybody is a multi-path, then you are going to see the namespace head state table, and this is how it would look. So, here.
The list of namespacer, so when yeah. How do you get the numbers for the performance? Do you look at a host or what do you use it? Okay, so this is yes. So in the sys block disk name slash
There's a state, right, which your block layer exports. So, this we calculate from that. Yeah. Okay. So, here in this table, the first column is about the namespace head. What is the namespace ID associated to the head? Then, number of paths.
Namespace that would provide to reach to your destination. So, if you see here for NVMe 7 and 2, the number of paths are 2 because probably I have configured a state namespace and connected behind two controllers, right? Then there are some more states, the RICU IO and fail IO. So, what is the RICUIO? So, the RICUIO will give you the information.
About the number of I/Os which has to be recued due to none of the paths which are available is could currently serve the I/O, okay, for some reason. So, today requiu I/O and failed I/O, these are the two states which are not exposed by the Linux kernel and MAB driver, but to show this, yes, we
May need to, we have to change some code. It requires some code changes in the Linux control just to expose this information for anywhere we talk to, you know, list it here. So, how many times do you have to reque the IO because none of the parts are right now could be able to perform either? Similarly, if none of the parts are currently available.
Or all parts are down, everything is gone. We have to force fail I/O. So that is the fail I/O counter. And then regular, your IOPS latency, bandwidth, number of in-flights I/Os, and the utilization. Again, here, all these IOPS latency bandwidth are aggregated at the namespace level.
Are the namespace head layer? Okay. Now, okay, the next screen. Just after the namespace head state table, we will have the path performance table. Okay. Now, here you will see the namespace head state.
Namespace ID, namespace path. So now you are going to further drill down and look all the statistics at the path level. Now, here you see there is a dotted arrow. So I added the dotted arrow just to notice.
For the user or administrator. So, it noticed just to make it more visible or just to make it easily viewable that this NVMe 7N2 is having a multiple path. So, right now, when on my system, when I took the snapshot, I only have a two-path, but if you have multiple paths under 7N2, NVMe 72222222222.
And two namespace head still, then there will be more such arrows down it. So basically, the same namespace head will be repeated again for all your individual paths which are catered by that namespace head. So, here this is what you will see: NVMe7, C3, N2, and blah blah blah, whatever it is. Now, next screen column is about nodes.
So, this nodes, because my I/O policy, if you see in the previous screen, so my IO policy is the NUMA, right? And so, you are also seeing the node screen. So, this is what is this node? So, node is nothing but a NUMA nodes, and it indicates that any I/O workload running on this NUMA.
Node 0 is going to utilize the path NVMe 7C3N2. If the I/O is originating from node 2 or 3 on the system, this is the path NVMe 7C7N2, it's going to be utilized to forward the I/O. And also, there is a controller table, controller column, which
Says behind which controller this path is associated. And then per path, IOPS latency, bandwidth, number of in-flights counter, and current utilization. So when I took the snapshot, probably I do not have any traffic running on first two paths. So all counters, IOPS latency.
Bandwidth are zero. But for this path, NVMe 7, C7, N2, there were some IOS IOs running, and so it is showing some numbers here. Moving on, then after that, there is one more table which we are going to show you, and that is the path.
Path health table on the dashboard. So now it is going to show you the path health per namespace path. The first column is about the ANA state of your path, whether it be optimized, unoptimized, inaccessible. So all those states. Then the next column is about the retrace counter. So per path, if I'm sending some IOR command and if
I get some error status back from the controller, right? I may retry. So, in the current implementation of NMME driver, we retry up to five times. Even after five times, if the command fails, we send the error status back to the user, right? So, this is the retry counter. So, retry counter would be incremented whenever we have to retry some.
Command or IO. Then there is a failover. So, how many times I have to failover from or switch from one part to the other path? Because one of my current paths, due to maybe latency or some issue in the intermediate switch, if it is an EMA or fabric, I'm not able to send IO. And so I may have to switch the part from.
Kernel path to the other path. So we would increment this failover counter and that value we would show you. Where is the failovers account coming? Yeah, so today, yeah, so today this retries failovers and errors counters are not exposed to the user. But in the kernel code, right, when we switch over, when we failover, in case of enemy multiple.
Path, right? My proposal is to add that counter exposed through C surface. And so in NEVME talk, we can show it. So you know this NEVME multi-path code when we have to send the IO to a path. If the path cannot do the IO right now, right? Or maybe you send I/O, but you get the written error.
Back some path error, for example, and then path error says that it is the error where I'm not able to send the IO on this path. We switch over to other active path if there is a path available. So that is the failover. So this common was just a placeholder and I is actually not okay. Yeah, today there is no counter, we are maintaining, but the path.
Proposal is, you know, we add that counter to make it useful for the user. Similarly, the errors counter. And that will be useful for so many other things. Because currently, we do have an issue that we can't recontinuous be context. So if, say, if we are running present logic,
But we continuously reconnect the controller and we completely miss that. We have no way of tracking the code to it. We have to total our game teleport, we can counter the controller itself, we prepare to track anything. Sounds useful.
Then there is one more table. Yeah, so this is a controller summary table, which you will see on the second dashboard. So, this table will give you the stat aggregated at the controller level. Okay, so the first column is the controller name, then, the number of paths associated to this controller, the node. So, here, what is the
Of node that is the NUMA node local to the controller. Okay. Then target type: if it is TCP, then you will see here TCP because when I took a snapshot, it was a PCIe, it is showing you the PCIe. What is the PCI address? If it is TCP, it is going to show you all the TCP addresses here. What is my current controller state?
Resetting, connecting, etc. That will come in this column. Then the regular values for IOPS, latency, bandwidth, and utilization. Again, this counter at this controller summary level would be aggregated per controller. So, yesterday when I was speaking about this, I think Hannes has a point that we
should also show probably if my controller has to reset how many times reset happen right how many times my controller has to reset for whatever reason okay and so if the status the current state of the controller is reset maybe we would add one more column here also
So, for the reset, we will count the number of times the controller has to reset. And similarly, if my controller has to be reconnected, if it is NVMFTC, for example, right, how many times I have to reconnect, something like that might be useful.
Sorry. How can you get utilized higher than 100%? So, this utilization is because, yeah, it should not go to more than 100%, but it is the formula. In fact, if you use the I/O state also, sometimes it goes 100 points or something, right? So, this is the maximum DIX utilization.
In percentage among all the paths, right? So if I am having multiple paths, I am just using at the controller level the max utilization for every path. And for each path, the utilization is calculated like you know fraction of time the controller was busy serving the I/O. It is all
Of floating point counters, right? So it may not be exactly match 100%, sometime plus or minus some point. Okay, so this is the controller summary screen. And now, in the next screen, I'm going to show you the complete dashboard. It is coming. So, all this information taken together.
This is how your final dashboard would look like. Okay, so at the top, as I mentioned, you will see the header for your subsystem, which also shows you the so this is your subsystem header. It shows you what is my current I/O policy. Now, at the runtime, if the user changes the IO policy from NUMA to Round Robin or QLAPS,
It will be updated here. And also, based on the I/O policy value, we will also update some of the column here. For example, for my I/O policy QDepth, this nodes column under path performance doesn't make sense, right? So, this column would be replaced with the Q depth in real time.
Because if your IO policy is queue depth, I want to know what is my current depth of the queue for that path, right? So that we will change in the runtime. So, yeah, coming back to the dashboard. So, you see here the header, namespace head state, followed by path performance states, followed by your path.
Path health and followed by controller summary. Again, this screen is also interactive. So at the end, you will see if you want to not monitor this subsystem, you press escape, you go back to the level one screen. You may choose another subsystem for monitoring, or you want to quit directly from here, you can press.
As well, here. So, this is how the final dashboard would look like to the user. Okay, now this is all about the dashboard for the multipath. Now, if we have not configured the multipath, right, then how the dashboard would the second-level dashboard would look like? The first level dashboard would look similar.
But the second level dashboard for the non-multipath case would look like this. Okay. So if you are not configuring the multipath, then it doesn't make sense to have you know namespace head state or your path state because now I do not have a multipath. So we would in that case only
Show two tables. Yeah, the first at the top header would remain the same. The first line is the refresh interval, whatever refresh interval you choose, it could be one second, two seconds, or ten seconds, or whatever. Then your subsystem header, followed by your namespace state, and followed by the controller summary. So it's
You could see here, I have taken the snapshot for NVMF TCP. So I could see my target type is TCP, what is my address, then regular information about the controller state, IOPS latency, bandwidth, and utilization. So, this is how for a non-multi-path case, the screen would look like.
Okay, so some implementation detail I just want to call out here. So, when I did the POC for NVIDIA, I used pure term IOS-based UI, no end curses, because if you use the NCERI,
Yeah, it would definitely work. But because this is a simple dashboard and we don't want probably fancy stuff here for the dashboard, I choose not to use the N curses, but just use the term IOS. Also, the other advantage of not using NCUSIS is that the current plan is to integrate this tool as a part.
Of NVMe CLI, right? And so we don't need to link the NVMe CLI with any external libraries. If we just use TermIOS, which is supported inbuilt in the GLIPC today. So, not any purses, it's a simple deployment, no external libraries required. And also, with the Term IOS, we could.
Redraw the screen really fast. And as I mentioned, it is an advantage, it is the easier integration into NVMe CLI. And I use for the POC the ANSI escape sequence codes for cursor movement, screen clearing, and your row highlighting.
And this tool, based on the interval you choose, based on the delta, it retraws the screen. So if you choose the delta two seconds, you will see all the states updated for every two seconds. If it is one second, updated for every one second. And states are collected through C-SafS or Libian UMAN IOTL.
Okay, so now, yeah, I just want to get the feedback from the community here. If you can tell me, if whatever metrics I have shown for NVMe top, is there any metrics you think are missing, redundant? You want to add something, you want to remove something, or any other feedback?
As to that, what would be extremely hard would be a Prometheus or hex report. As you already gather all the statistics, it would be extremely cool to actually save the statistic to someone where the data is in data that one can do that analysis later on. That would be extremely hard.
Okay, sure. Yeah. Yeah, so Grafana export. Yeah, Dennis also agreed with that. I think, yeah, we would work on that to also export this data to Grafana. So, any suggestion for the interactive feature? So, right now for interaction, you have up and down arrow key, escape.
And Q for fit. Any any other interaction you would like to add? So and it might have also helped look while adding error codes to the controller. So we already have that there, but it would also be an idea to look for connect errors. That's one of the issues Danius Graham is going to be struggling with.
trying to figure out how to reconcile this various methods of goal, NDB connector or NDB connect sample to the transport. And um if we had a counter for any connect errors, we could actually figure out whether connect adds the CSS here or not, how does the sample left in the dark.
Yeah, makes sense, yes. Okay, so so so uh i it's so so what you are uh you are ask is that in the controller summary table for some of the state like this is uh this is more the um basic current hashes sound, because all the numbers that you know are called Windows.
Not there at all. So we need to do adding these counters. And while added, we might as well add another counter for reconnectors. Yes. Because that is something which isn't tracked dropping currently. What we do is just we track the number of reconnects, but we do not track the reconnect.
Actually, succeed it all. Okay, yeah. And also, this counter should be aggregated, not the data counters, right? Yeah, sure. I mean, well, obviously, the point is just telling you all that didn't work, but I know I mean trying again, and then you raise the counter again. Yeah, it makes sense to have aggregate them.
Throughout your dashboard, throughout your dashboard. The mom moment you start the dashboard, we show you the aggregated value which is exported by the C surface, right? So every time you reset, we increment the counter, but that counter value will be stored in C surface. But when you start the dashboard, it will show you what is the current value of that counter.
A another one thing to sit is whether we want to have the countless and scissors button in the opening. Um because we start I wouldn't say opening, but show at least crawling the scissors that
A local stop from the system. I'm not sure whether we want to get climate further. There's an audio problem. They can't hear anything we're saying. We have to speak close to the mic. Alistair, can you?
Well, we're practically.
Going into the mind, so sorry, like there must be an audio problem. Yeah, so this question which Hannes is asking is whether we should expose those counters, NVMe counters, through C-surface, because C-surface is already polluted within.
Lot of things. So, where we can put those counter if not in CSS? It's not politicians, it's just an interface between the interface from the kernel. So, it's just going to be more you use them or not, that's your choice. I mean, it doesn't cost you anything to have in CSFS even if you don't use them.
I'm not concerned about the Lucian CFS. It's not it's meant to be programmed pro programmatically parsed. So if it hurts your eyes to look at it, it don't look. I have a lot of talk about any copical value and so I guess why?
Prime case would be for out of benching where we just have with anything we had any commands going on. Is that is that possible to use with this design? Yeah, I mean, we do use uh admin commands, but all these counters, right? Yeah, yeah, maybe that's a good question.
Just that that's exactly is essentially that is scraping. So it's not that the accounts themselves are looking really little MDV. Because last I think on the very on one very very first slide, one MVV is actually pretty standard, all the information in there. And because essentially
The current setup or design of Lit MBB is that it's essentially one shot. So you call it once for a specific operation, then everything's being built up, then you have your result, and everything is completely different. So it's not really meant to be a long-looked thing, as it were.
Easy to design and write, but less easy to use to go to a longer application, even for a lot of what are using before. Yeah, we have to structure it. So we have out of bands wildering anyway.
device inspection which you use with NVMe MI both the same interface just uh instantly software but live NVMe MI would it work with NBME fabrics would that have graphana export and all yeah because I thought that does also
The back end has no s access, of course, right? Yeah, certainly. But even the right time. I mean, Andrew is a certain one, like the day and the market is loaded with the next board. Cool. But not the time to say, but that's certainly existing. I don't have a look that would be an extra place, but it doesn't matter.
Yeah. So anyway, Grafana we are going to do. We will export this information. Even start with consuming the I guess the monitoring within the burger.
So you have the read light statistics on the thing. I would also recommend having a discard. Discard. That's that yeah, that's there's at least ten items I can
Definitely change the performance. So seeing windows are happening to be so read, write, and discard state. We can also add for flush as well. You can. Yeah, sure. I mean, if you have it, if it required, but okay. I don't think flush is actually handed in the stats. So flush is part of.
It is in yeah sniper latency how you're at theirs as well. So it's looking good really big to the point.
Is that we can add more and more things, but also we need to think about you know one other thing: the size or space or the real estate you have available on your screen, so that is one other challenge, you know. Uh, but we can add certainly, yeah, uh, okay.
So, other thing is that I was having when I took the snapshot only three disks, three or four disks attached, right? So, everything I was able to, you know, easily fit in one screen. But assume you have so many disks attached to your host, right? How can we handle that? So, I mean, this.
This is my idea. You may chime in or recommend. So, what I was thinking about, if you see this screen, right, here for user interaction, we only have the escape button and the queue is for queue it button, right? But if I see that I have a lot of disks or I have a lot of namespaces created on my disk.
Right, your namespace state table, or maybe path state here. Yeah, this path performance, or maybe the namespace head state, or some of this table may feel up your screen, you won't be able to see everything. So, how about adding some more interaction, user interaction here? Let's say I add one more button.
Option for the users, press C to toggle controller summary. So I don't want to look at controller summary. You just press C, you would be able to toggle that table. Similarly, press H for path health, press V for path performance, and maybe press N for namespace hex state. You just would be able to toggle.
So, you choose which table is most important for you, and only you want to see it or focus on that. Right? Sorry, I got last. You are talking about a single subsystem that you want to look at, or multiple other systems? No, on the same dashboard, assume that, I choose one subsystem.
In the subsystem, I have so many namespaces created, right? Just so that's what I was going to say. What about something like a h stop, those disks? Yeah. You can't think about a lot of them on a single screen screen.
then look at actually different voices at the same time okay yeah for example if you take the top today you will have augmented some process but from CPU perspective whatever has to you top implied just to show it will show on the top yeah
Which shows Kamo, and then it's up to the user to go at best commode if they want to. We can do something similar. Okay, so only show the disk which are doing some I/O on the top, right? The namespace disk where the active I/O is running, showing at the top, and for
The namespace disks which are not doing active I/O, where your utilization would be almost zero, it should so it should be shorted by probably utilization or the IOPS or whatever with some numbers. Yeah, that could be also done.
I'm not sure, excuse me again. Disk, the namespace is a disk for all devices. So you probably want to resolve that at the top, even if there's no IO, because seeing that there are no ILO is also an important thing. Okay. And then maybe whatever the substance calls.
for all quality in the east hand yeah yeah yeah so here the problem statement is i think bit different compared to top process state or process when you see i'm talking the something like edge top plus where you see all the disk you uh at the the top.
Okay. And details below. Okay, got it. So you want to. There is a kind of a graphical summary, parts, not numbers. But it's super easy to just look at it and grasp what's happening to all your devices, block devices, which for the market. So my column, column should.
Should be the block device name. What we are seeing. On the top, you see the block device, and then on the so your column is the block device, and your rows are all the states. That is how you want to organize? So just try half. Okay. Then look at the purpose of block device and
Switch between CPU, one by CC. Ah, but there's a lot of different screens, but the representation is super easy to do. Okay, got it, got it. Oh, yeah. Call edge stop. Edge talk, yeah. Okay.
For example, stop when you process the legacy feeder. Maybe you can do it. Uh and then uh for the lib LVME API, as we know, uh
So, today, the way libnvm is designed, it would just give you the cached value. When you build the topology in memory, you cache all those parameters or state value. And if you again query the same API, it will give you the cache number, right? So, another proposal is that either we update those APIs.
So that it will always fetch the latest value, or introduce a new API, which could be right. So, you what you have update the existing API or introduce a new API to get the latest value. Whenever I query, I get what is the correct.
I think introduce an API to refresh. Okay. This that's my initial team group reaction, so maybe it's not the best idea that's how I'm doing it on it.
Yeah, so I think Daniel told me that NVMe CLI 3.0 is currently under work and it is going to be released in maybe some days or maybe after some months. So there we can change the existing API also, whatever way we want. So anyway.
Because if you update the existing API, it would work for both your single snapshot use case, which today is what NVMe CLI do, and of course for NBAM VTOP, because you query the API, you get whatever is the current value. Anyway, for NBAMV CLI, single snapshot output, you only query it once and display it, right?
But if I try to requery the same API, it will give me what is the latest value. So instead of introducing new API, we can just reuse the same API and update the code to fetch the current value. And then another
Another question is polling versus notification-based models, right? So today, whatever we do is kind of for anywhere we talk, we always go and poll the bell. Basically, you reread the C-surface counters and display it. But instead of that,
How about kernel sending some event? Okay, for example, I notify or FA notify. Whenever the counter is updated, you send that event to user space and you go and update the value instead of pulling it every second. Not for I go, but for other things like
Not for IO. Not for IO. Not for IO, yeah. Like use Delta algorithm. Like like you know, your here's your algorithm. That could be like yeah, that could be a notified do not use new events ever. Okay. They are um they're inverse because it's
Because we do have terrible issues currently with you that not being able to process all the events. And if you start using that tools, these kind of events will overload even more. So it will never ever be able to complete all the events. So don't do this. And similarly here, trying to get for a push-based model.
is a bit just no um that was just actually a too much of an open because um trying to do things with i notify you can quickly find that yeah it worked on that files not on the other one and then the other one is um scissor face does yeah it does support ever and i note but then
The other problem is that the set of functionality could be an I-notify and F-I-nodify this two point is jumped. So you find this that now you can use both which and it's it's functional trying to send the microphone. Okay, we may miss some event and also create unnecessary complexities.
Sure. Uh and also I hope that it is good to keep the UI simple by using TOMIOS. Uh we don't want to add anchors for this dashboard, right?
I should keep it simple as it were. If someone wants to have anything more complex and more configurable, go for the hard button and tell them my closer one. Okay. Ah, okay. Yeah, when I was working on the POC, I also discovered that the Q depth value.
Which we today maintain if your I/O policy is Q-depth, right? To choose the path based on what is my current Q-depth, you choose the path which has the less number of IOs Q-depth, right? That is per controller, not per path level. And today we expose that through C-safes per path. Ideally, it should.
be exposed under sea surface controller. Okay. Yeah, so what I was saying here let me go to one another slide. Okay.
It's not here. So that's the remote. That's confusing. You just take controller and number of parts on that controller. So here, okay, I took the snapshot for the NUMA, but what I'm saying is if your I/O policy is QDAT, right, we would replace.
This north column under path performance with a Q dat, right? Okay. So you see here this North column would be replaced.
With the Qdepth colour, whenever your IO policy says Qdep. What I'm saying, we maintain the Qdepth per controller, not per path. Well, you know, but the way it's different to the controller path, that's the difference. The control is the path. Yeah, right, you are right, right? But today, this kernel is exposing.
Under per path. So if you go to C surface, block your path block name, and then you have a QDAT. Instead of that, it should be sys class NBNB, your NBNB device name, NBA controller name, slash Qdath.
How it should be. Okay. And so once we do that, I think we need to fix that. Under controller summary, we can add one more column for that keyboard. Yes, yes. So accordingly, this dashboard will be also updated. Okay.
Then not able to see if you go back to the week one and a little bit of a map.
The first nestball? So any life updates, live anyparts, so that's a bit of a challenge because normally what we do is we're building up a copy or a representation.
Offices have a street memory. And currently there is still no synchronization protection anything around it. It's just the tree in memory. If we want to somehow make this long live, we have to put in some sort of well aggregate mechanism in there.
which again, which mirrors the changes to system has been to the entire representation which is which means you have some sort of tree synchronization update mechanism to ensure that you update the tree while no one else is accessing it because
Otherwise anyone else except in the tree at that time might be going to whatever don't you simple operation and I'm not sure whether we should go with that. So on one terminal you are doing NVMe top other terminal and you are running NVNC like a market and if you need some synchronization in GitHub. But uh but this is two separate process, right?
It is not going to overwrite anything. And of course, we are not updating the tree. We are just building a tree and then reading the information, right? And again, if you are doing that on one terminal, I'm running any talk, other terminal, I'm doing any CLI some stuff, right? Both are two separate processes. So they have their own copy.
If you go to monitor events in any VTO, you are actually constantly updating and re-renewing the internal tree to ensure that we do not miss updates from the service. We are not rebuilding the tree. We are not rebuilding the complete tree for any we talk. Once the tree is rebuilt, we are just monitoring the tree and
When any changes happen for some of those parameters, we go and update those parameters. But what we are saying, maybe if I'm removing some namespace or adding a new namespace during that window, we may miss it and we need to rebuild the tree anyway, always for NVMe talk as well.
Okay, I'm not I'm quite convinced there, but they will be I mean your your concern is if synchronization is needed. Yeah, I n not much synchronization. The my problem here is if you don't if you just keep it static in in memory.
We eventually will run out of symptom with the system as a representation. But at the very least, you'll need to catch the new events to ensure that you allow us to figure out that something has changed on the system of things. So, at the very last very stage, we should need basically some sort of new event which helps.
Something has changed, so the values they should be doing between now. Okay, got it. Okay, yeah. Yeah, so for example, new namespace is added or namespace is deleted. I need to have some event, so I recreate the tree, rebuild the tree, right? Okay, yeah. So for that, we need the event, which we don't have today, at least for those. Yeah, got it.
Just be invented as listeners, and you don't have to act on the pod events, you just have to track the lens. Yeah, okay. So then, as I mentioned, so we will also modify the Kernos source code to expose this counter failover errors, requiem IOs, and fail IOs. And yeah, I think that's all.
Uh so in summary, uh I hope that the NVMe to uh tool would pro would help the users and administrator to you know fast rise any issues with the NVMe PCR fabric and also uh it should help the people to just observe or monitor what is going on behind the scene for my NVMe.
Disk, even if it is a multi-part disk, in fact, it will give you some better view to see what is happening. And so, eventually, the goal is to shape its features and direction together with the community and your feedback. So, please let me know. Maybe we are running out of time, so I'm going to stop.
Presentation. If you have any feedback during the hallway trek, you can meet me and talk to me if you have any feedback about the NVMe talk. So, thank you very much. So, your feedback and suggestions are most welcome to shape the tool.