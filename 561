Keynote___Using_P4_NICs_for_Resilient_Scale_out_GPU_Interconnect

Please welcome Krishna. Thank you. Yes, it's very nice to see all of you. Thank you for inviting me for this keynote. So, today I'll talk about how we want to use P4 Next for AI skills.
Scale out networks. Oh, this one. So, I mean, I am sure this slide is not a surprise for you guys. The model scale is growing exponentially.
We are in now trillions of parameters. So, what does that require with respect to trillions of parameters? If you think about what AI workloads are, especially training, they are bulk synchronous workloads, pretty much in everything working in unison in one GPU cluster. So, since the parameters of scale is going to trillions.
Of parameters that requires a huge GPU clusters. I mean, we were talking about 10,000 clusters, and you know, publicly 100,000 clusters have been deployed, and now we are talking about a million GPUs in one AI cluster. So, what does that require to support 1 million GPU clusters? It may
It so happened that all of the GPUs are not even in one data center building. As you know, I mean, probably you read that the biggest problem in data center scale is power. And in one building, you may not fit, obviously, not fill a million GPUs. So, you may need to spread out the GPU cluster across multiple buildings.
And sometimes even across the WAN to go through this huge scale of GPU clusters. I mean, this obviously brings in a lot of innovation that has to be done at all layers of the stack. It has to be done at the algorithmic level, and you know, how you do parallels, data, expert, tensor, and a lot of papers have been written about that.
It's also important to understand that it also gives us a unique requirement on the networking infrastructure. So, I'm going to talk about what are the traits that are required with respect to scale-out GPU interconnect. So, on the highest level, I'm sure all of you guys are familiar with AI training of workloads, work on top.
Of collective libraries called CCLs. The CCL, if you want a GPU pair to talk together, it creates a concept of RDMAQ pair, that is how it interacts with the NIC. And the RDMAQ pair, for a network point of view, it translates to a fightable basically with a UDP source.
Port and it so happens that if you have multiple GPUs talking to each other through multiple NICs and all of them potentially use the same phytuple or different phytuple for especially for the source port, it can hash into the Tor to spine link, there will be congestion or oversubscription. If you think about it, the network itself is not oversubscriptuple.
Subscribed is just that some links because it is hash collision between torque and spine will get overused and the other links are you know not at all used. So, this can be solved using multipathing. The concept of multipathing is basically between GPU to GPU, these are typically high bandwidth flows and long bandwidth.
Living flows. This high bandwidth flows instead of using one canonical network flow, you split into multiple flows, and those multiple flows can be spread across between tort to spine on multiple links. That is the idea of multipathing. I will talk about how P4 can help in the future slides, but it is also.
Also important to understand this multipathing, if you are inside one data center clause, it is easier to do it. I mean, we have been doing in front-end networks in DPU world for a long time. But here, if the bandwidth is spread across multiple data centers and you have non-uniform bandwidth and different latencies.
This multipathing gets tricky. Also, if you look at you know the RDMA before AI, it has been deployed in Ethernet world for storage networks. And in those clusters, typically smaller clusters, not as big as clusters as AI clusters that we are talking about here.
Clusters typically you deploy something called PFC plus DCQCN, which doesn't scale. I mean, there have been a lot of papers written primarily by Microsoft, Alibaba, and all other hyperscalers. You have live lock, you have dead lock, and all those kinds of issues. So, typically, people don't deploy PFC across the data center because you have all
These issues. And as I was talking about a scale across, which is basically scaling a GPU cluster across multiple data centers, you cannot even do PFC because it's a shared infrastructure and you cannot tune ECNs, which is very complicated as is. So, what you require in this AI clusters if you want to scale is you need to support.
About lossy networks. Packets can get dropped that standard Ethernet, TCP works, you know, on internet scale. We can make it work on a GPU cluster scale of 1 million when there is loss. Also, in talking about loss, if you think about loss in RDMA standard networks, there is this concept.
Concept called go back in, which means that if you drop some packets, you have to go back and retransmit all those packets. It is not really network efficient, that is not how internet works today. If you look at TCP, there is something concept called selective acknowledgement SAC. So, there is a need to do those kinds of enhancements.
Into AI networks. And you know, if you look at this clusters, I mean, there has been talk for 1 gigawatt takes 50 billion dollars, which is a lot of money that you are paying for this GPU clusters. So, wherever we can do TCO optimal solution, it is going to show up at scale. So, it is
Very important to support TCO optimal networking topologies that can help these problems of scaling AI networks. Moving forward, right? I mean, obviously, things at scale break. I mean, that's what we learned in the last 15 years of cloud deployments when you have.
Have 100k GPU cluster, which is not the scale that I am talking about. You have lots of switches, you have lots of transceivers, you have a lot of optical cables, lot of links, and all of these things fail all the time. I mean, link can fail, switch can fail, NIC can fail, GPU can fail, and remember all these have deep software stacks. I am not talking just about hardware.
Failures, you know, NIC has a stack, you know, switches have stacks, and GPU has a deep stack too because you have the GPU drivers, you have the CCL libraries, you have PyTorch, and all of those things that are running on top of it. So, when you have this much failures and you are paying so much for a GPU cluster, you want to make sure that your infrastructure, particularly the network infrastructure,
Infrastructure is resilient. That is an important characteristic that you would want to scale this AI fabrics. So, given that we are at P4 conference, right, you know, these are the problems that we have in AI networks. Where does P4 come in? I mean, if you take a step back from P4, right, you know, the P4 was invented.
At a time when we were talking about multiple network encapsulations, we have, you know, at that time we were talking about VXLAN, we were talking about NVGRE, and every cloud provider or even enterprise customer wanted to do his own P4 pipeline. Obviously, it was an evolution of OpenFlow kind of thing that you can do data path programmability.
So that got some traction, but where P4 really got traction, at least in the deployment world, is in the NICS space, in the P4, NICs, and DPU space. The reason for that is very simple. I mean, for example, as Pensando, we deployed a bunch of DPUs in multiple cloud providers at some, at half.
A million scales that's running every second of the day running workloads. So, what happens in those workloads is in this cloud providers is that they have different SDN stacks, they have different encapsulation, they have different firewall ways of doing micro-segmentation, they have different load balancing. So, all that is encapsulated in this.
P4NET block and also people do not follow one encryption format. Some people do PSP, some people do IPsec, tunnel transport mode, and some people do their own encapsulation format on top of VXLAN. So, this programmability has been solved and in deployment at
Very high scale in the cloud providers today. So, that you could argue it's a natural evolution of P4, but where Pensando did more innovation on top of standard network packet in packet out is where we have programmability from descriptor in packet out.
A different way of thinking about it. So that's what we call a P4 DMA block. It's a classic example. You could have your own Ethernet driver, but let's say you want to deploy a NIC, which is a common Ethernet driver, which, like in NVMe, it exists, and Ethernet doesn't exist, but you want to do Word.io, for example. You can have your own descriptor format and you.
Can have your own stateful translation of that descriptor and write a P4 program to do Ethernet driver processing. So that's where P4 DMA was invented. And obviously, when we move forward today, storage required similar innovation. I know this NVMe descriptor format is fixed, but some customers wanted.
NVME TCP, some customers wanted NVMe or DMA, and some customers, you know, if you think from a storage point of view, you do encryption first and then compression, then compression encryption. So, all those service chaining things that you want, you want programmability that we had in the networking world. So, that's what P4DMA was trying to do. Now, moving towards AI networks.
Networks, what are AI networks? As I said, it's based off Rocky V2 RDMA. But just like when we were having the evolution of this network encapsulations and storage encapsulations and firewalls and all those things, similar thing is happening in the AI world right now. We are all going.
Toward a standard called UVC, but customers are not waiting. Hyperscalers are doing their own RDMA transports, various RDMA transports with solving the same traits that I talked about earlier for multi-pathing cases, for congestion management, for telemetry, and how you do multi-plane, which I'll talk about.
So, that is where we are reusing P4, whatever we built in the front end NICs and DPUs, which we are deployed at scale. We are using the same technology to repurpose it or write new P4 programs for you know AI NIC scale-out networks. So, moving forward, so how you know.
Now I just want to give few examples of you know how we are using P4NIX. As I was saying earlier, right, you know, you could between a GPU to GPU, if you are sending a big flow and if there is multiple flows colliding on a tor to spine, there is congestion. I mean how do you solve it? I mean there are multiple ways of solving it and there is no one way of solving it.
I mean, that's where P4 programmability will come in. I'll go through some examples of how you solve it. First example is basically today, if you talk to any switch vendor, we would do something called dynamic load balancing packet spray. So, in that mode, what happens is that you take packets from one flow between GPU to GPU and spray it in the network.
You know, from between tort to spine, you have local, you know, which queues are occupied, which queues are not occupied, and you spray it to multiple spines. So that works, but as you can imagine, different links in the network topology have different latencies, right? And you have different congestion. So, what that causes is that the
Same flow between GPU to GPU and you're spraying, you could cause out-of-order packets coming to the NIC. Remember that GPU doesn't understand anything about networking. GPU doesn't understand that, hey, I'm moving from data from point X to point Y. If you get out of order things, basically you're corrupting the data. So, what in this case, what
The NICs do in this topology is that NICs will do out-of-order processing and make sure that packets, all the packets arrive in the right order that they were sent and then give a notification to the GPU for data arrive. So that's one way of doing it. The other way is, you know, so-called the UEC way: hey, instead of you know what the problem is.
Is you need to give more entropy to network using ECMP. For one flow, if you have one UDP source port, that's not enough. So, how about create 128 or 256 source ports between the same queue pair? So, that's what the entropy-based load balancing is. So, then you know that you have.
Have enough entropy, all the links between dot to spines are used. So that's one way. And you know, it gets tricky because obviously, as you were saying earlier, if everything is in one superspine, it's okay. But now you're across buildings, you have to make sure that when you're load balancing, you have to take account into different variables.
Network bandwidth lengths and latency. So, that's that's but you could do that. I mean, that's what US is recommending. The other way is basically source routing, which I'll talk through that. I mean, it's not new for networking force. I mean, people talk about traffic engineering and source routing. So, here the controller gives us a set of labels, and using those labels, you could.
I mean, you could still still spray to set up certain set of entropies, and those entropies take a fixed path inside the network. Or, you know, some customers want to do their own congestion, own load balancing scheme. They want to say, hey, I want to take some paths, high-priority paths for some traffic, and some I want to take low priority paths. You know, those kind of
Things that you would want to do. You want to give programmability to customers to do this because this is an evolving market, as I was saying earlier. That's where P4 shines. I mean, while we're at it, other things that P4 has been very helpful in the front-end NICs and the DPU spaces, you know, deploying it and operationalizing it. So, whatever you're talking about.
In the lab, in 1000 or even 2000 servers, right, things work, but when you go to half a million servers and million servers, things break. We know that. But if you do hard-coded ASICs, the problem with that is that you might not capture the metric that you would require. But if you have an architecture where
You could change what counters you're adding for a specific case that you're debugging, and you can live upgrade that P4 in a live network without traffic disruption and then check what the issue is. That gives a very powerful construct for cloud providers because most of the time is not spent in development, most of the time is spent in.
Operations. So that's, you know, but anyway, I'm going to give you a sense of where P4 we are using, you know, when we are developing these features. The other thing I talked about TCO, I mean, if you are trying to deploy this large-scale networks, cost matters. One way of deploying is basically
Basically, when you have torque to spine link, for example, 400 or 800, you can connect to a 400-gig switch, okay, that we call in our terminology in this slide, we call it single-plane. If you can imagine switches that are built with 400 or 800, if you just do with 400, the number of ports are
Whatever it says, n, but if you break out that 400 switch port into 100 gig, you can do 4n types, the 4n type, 4n number, 4n number of ports. Actually, why is that useful? If you think about it, right, if you have a bigger radix of a switch, you can build smaller class fabrics. That means what you can
Do from the NIC perspective is instead of taking one 400, you break it up into 400 gigs and connect to 4 different planes. We call it multi-plane, and in that multi-plane, you would do 100-gig deployment. You use the switches in 100 gig. So, as an example, cluster, since we are talking about 100,000 GPU clusters, 100,000 GPUs.
GPU clusters typically, if you deploy at 400 gig network, you would need 5000 switches. If you deploy in the multi-plane format of 100 gig, you would need 3000 switches. Obviously, you know, that is 40 percent CapEx saving, and now talk about the operation thing: power and optics, and you know, cable replacement, if you add all those things up.
It is a significant performance and cost optimization. At the same time, it gives added benefit of not just TCO. If you think about it now, from the NIC point of view, you have multiple planes, and if one plane goes down or one plane is congested, it does not affect the other three planes. So, where does P4 come in here?
Now, earlier I was talking about multi-path. One GPU to GPU flow, it sprays across multiple entropies, it creates multiple source ports and creates sprays it into multiple flows as far as the network is concerned. Now, not only does it have to do that, it also has to spray across multiple ports. So, now we can think about it.
You can have two-level load balancing that's required in a real network. I mean, it's not that these things can be thought through earlier. Some of this is, as I said, dynamic, and this was brought up, the paper that I put here by DeepSeek is the optimal way to build networks, and P4 can help you adapt to the things that you don't know and that it.
Will come up in the future. Okay, so we talked a lot about reliability. Right now, you have this two-level load balancing, you have multiple paths, and you have multiple planes, but things do go wrong. And you know, from the NIC perspective, how do you know that you should avoid some paths? So, what we do
Do we use P4-based programmability again? You know, we send probes at hardware speeds to figure out which path is down, and those paths we avoid spraying traffic to it. So, basically, it also ties into the path aware congestion scheme that we do. It also helps with these probes, and then once we know that some links are permanent.
Down, we give a notification to the controller. Controller can come and adjust its own thing that it can do to avoid those things for higher priority traffic. So, this is still done completely in DataPath in P4. And the last example that I want to talk about: P4, using P4 resource.
I mean, this is very natural for us. I mean, source routing, we have been doing NCAPs. This is another fine form of NCAP. But why do you even need that? Earlier, I was saying that you have entropy, let's say, between flow to flow, you have 128 or 256 paths, but still you really don't have control, depending on the switch hashing function, what is the link between tort to spine you pick.
Don't control it. But let's say you know what you want from the application point of view when you're doing source routing. I want a deterministic path in the network. That's what you use for source routing. So, in this case, the network controller, in conjunction with the application, will tell you: hey, for this CCL workloads, for this.
Operation collective between this set of queue pairs use this set of labels. It could be SRV6 labels, it could be MPLS labels. Then, what happens is, from the network point of view, it is very deterministic, and when something goes down, the NIC already knows that it will use the other paths, and other paths also pick specified paths that are talked through.
The application. So, this is something that again is something that we evolved. Not all customers do that. I mean, basically, the advantage of programmability for us is that you have various customer requirements, you have various topologies that are deployed, some things are brown field, some things are green field.
And sometimes you are interoperated with partners. If you put all this matrix together, one solution does not fit all. So, we need multiple solutions, but it is not like we are going to build multiple P4 next because multiple next, not P4 next, to solve this one. So, we use P4 as the tool to solve this customer.
Customizability and adaptability into real-line networks. So, taking a step back of what we are trying to use P4 for, as I said, P4 has been deployed successfully in the Nixon DPU space, in the front-end networks, and in the DPUs for networks.
For SDN type of applications, for security type of applications, you know, for storage type of applications. Now we are introducing P4 for AI back-end networks using the innovations that we did in Pensando for P4 DMA. And all the things that have been talked about have been implemented by a very talented set of engineers.
Inside, you know, AMD networking group, and all of this have been deployed right now. Thank you.